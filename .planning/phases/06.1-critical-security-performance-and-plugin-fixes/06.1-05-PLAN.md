---
phase: 06.1-critical-security-performance-and-plugin-fixes
plan: 05
type: execute
wave: 3
depends_on: [06.1-01, 06.1-02, 06.1-03, 06.1-04]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All critical security issues verified as resolved"
    - "Performance optimizations measured and documented"
    - "No regressions in existing workflow functionality"
  artifacts:
    - path: ".planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md"
      provides: "Test results and verification evidence"
      contains: "Security Verification Results"
  key_links:
    - from: "Verification tests"
      to: "Modified scripts"
      via: "Automated and manual testing"
      pattern: "test.*pass"
---

<objective>
Verify all security fixes, performance optimizations, and plugin hardening are effective and complete.

Purpose: Provide evidence that critical vulnerabilities are resolved and performance improvements are measurable, without introducing regressions.

Output: Comprehensive verification report with test results, performance benchmarks, and approval checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-RESEARCH.md
@.planning/code_analysis/20260429_140009/A_SECURITY_PERFORMANCE.md
@.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-01-SUMMARY.md
@.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-02-SUMMARY.md
@.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-03-SUMMARY.md
@.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Run automated security and syntax validation</name>
  <files></files>
  <action>
Execute comprehensive automated verification suite:

**1. Shell script security scanning:**
```bash
# Scan modified shell script with ShellCheck
shellcheck -x scripts/com/1_pr_prod.sh > .planning/phases/06.1-critical-security-performance-and-plugin-fixes/shellcheck-results.txt 2>&1 || true

# Verify no critical issues (SC2086 command injection should be gone)
grep -i 'SC2086' .planning/phases/06.1-critical-security-performance-and-plugin-fixes/shellcheck-results.txt && echo "FAIL: SC2086 still present" || echo "PASS: Command injection fixed"
```

**2. Python syntax and import validation:**
```bash
# Compile all modified Python files
python -m py_compile scripts/infra/executor.py
python -m py_compile scripts/infra/plugins/conversion_cache.py
python -m py_compile scripts/dock/0_gro_itp_to_mol2.py
python -m py_compile scripts/com/3_com_ana_trj.py
python -m py_compile scripts/infra/plugins/workspace_init.py

echo "Python syntax validation: PASS"
```

**3. Import dependency check:**
```bash
# Verify all imports resolve correctly
python -c "import scripts.infra.executor" 2>&1 | grep -i error && echo "FAIL: executor imports" || echo "PASS: executor imports"
python -c "from scripts.infra.plugins import conversion_cache" 2>&1 | grep -i error && echo "FAIL: cache imports" || echo "PASS: cache imports"
python -c "from scripts.infra.plugins import workspace_init" 2>&1 | grep -i error && echo "FAIL: workspace imports" || echo "PASS: workspace imports"
```

**4. Generate verification report:**
Create `.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md` with:
- Test execution timestamp
- ShellCheck results summary
- Python compilation results
- Import validation results
- Issues found (if any)

Report structure:
```markdown
# Phase 6.1 Security & Performance Fixes - Verification Report

**Generated:** [timestamp]
**Phase:** 06.1-critical-security-performance-and-plugin-fixes

## Security Verification Results

### 1. Command Injection (Critical - Issue #1)
**File:** scripts/com/1_pr_prod.sh
**Test:** ShellCheck SC2086 scan
**Result:** [PASS/FAIL]
**Evidence:** [shellcheck output snippet]

### 2. TOCTOU Race Condition (Critical - Issue #3)
**File:** scripts/infra/executor.py
**Test:** Code review - retry logic present
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'for retry in range' output]

### 3. Unchecked File Operations (Critical - Issue #2)
**File:** scripts/infra/plugins/conversion_cache.py
**Test:** Code review - missing_ok=True present
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'unlink(missing_ok=True)' output]

### 4. Atom Mismatch Validation (Critical - Issue #4)
**File:** scripts/dock/0_gro_itp_to_mol2.py
**Test:** Code review - total count reporting present
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'len(missing_in_itp)' output]

## Performance Verification Results

### 5. Infinite Loop Prevention (High - Issue #5)
**File:** scripts/infra/executor.py
**Test:** Default timeout present
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'effective_timeout' output]

### 6. Contact Analysis Optimization (High - Issue #6)
**File:** scripts/com/3_com_ana_trj.py
**Test:** Frame striding implementation
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'stride = max(1' output]

### 7. Memory Leak Prevention (High - Issue #7)
**File:** scripts/com/3_com_ana_trj.py
**Test:** Try-finally cleanup present
**Result:** [PASS/FAIL]
**Evidence:** [grep count of 'finally:' blocks]

## Plugin Security Verification

### 8. Workspace Boundary Check (Plugin Issue #1)
**File:** scripts/infra/plugins/workspace_init.py
**Test:** is_relative_to validation present
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'is_relative_to(work_root)' output]

### 9. Security Documentation (Plugin Issue #2)
**File:** AGENTS.md
**Test:** Security section present
**Result:** [PASS/FAIL]
**Evidence:** [grep for 'Plugin and Hook Security Model' output]

## Python Validation

**Syntax Check:** [PASS/FAIL]
**Import Resolution:** [PASS/FAIL]

## Summary

**Total Issues Addressed:** 9/9
**Critical Issues Resolved:** 4/4
**High-Priority Issues Resolved:** 3/3
**Plugin Issues Resolved:** 2/2

**Status:** [READY FOR HUMAN REVIEW / NEEDS ATTENTION]
```
  </action>
  <verify>
```bash
# Verify verification report was created
test -f .planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md && echo "Verification report exists"

# Check report contains all sections
grep -q 'Security Verification Results' .planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md
grep -q 'Performance Verification Results' .planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md
grep -q 'Plugin Security Verification' .planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md
```
  </verify>
  <done>
- All automated tests executed and results captured
- ShellCheck confirms no SC2086 command injection warnings
- Python syntax validation passes for all modified files
- Verification report generated with structured test results
- Evidence provided for each security fix
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 6.1 security fixes, performance optimizations, and plugin hardening:
- Command injection elimination (SBATCH generation)
- TOCTOU race mitigation (Slurm status checking)
- File operation safety (cache clearing)
- Atom mismatch reporting (full count)
- Infinite loop prevention (default timeout)
- Contact analysis optimization (frame striding)
- Memory leak fixes (matplotlib cleanup)
- Workspace boundary validation (--force deletion)
- Security documentation (AGENTS.md)
  </what-built>
  
  <how-to-verify>
**Review verification report:**
1. Read `.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-VERIFICATION.md`
2. Confirm all 9 issues show PASS status
3. Review ShellCheck output for any remaining warnings

**Spot-check critical fixes:**
1. Inspect `scripts/com/1_pr_prod.sh` heredocs - should use `<<'EOF'` with sed substitution
2. Inspect `scripts/infra/executor.py` - should have retry loop and default timeout
3. Inspect `scripts/infra/plugins/conversion_cache.py` - should have `missing_ok=True`
4. Inspect `scripts/com/3_com_ana_trj.py` - should have frame striding and try-finally blocks

**Review documentation:**
1. Read new security section in AGENTS.md
2. Verify it covers workspace boundaries, command execution, file operations
3. Verify custom hook example is present and correct

**Check for regressions:**
1. No syntax errors in any modified files
2. All imports resolve correctly
3. Existing functionality preserved (no breaking changes to APIs)

**Expected outcomes:**
- ✅ All 9 security/performance issues resolved
- ✅ Verification report shows PASS for all tests
- ✅ ShellCheck clean (no SC2086)
- ✅ Python compiles without errors
- ✅ AGENTS.md has comprehensive security documentation
- ✅ No obvious regressions in workflow scripts
  </how-to-verify>
  
  <resume-signal>
Type "approved" if all fixes verified and no regressions found.
Type "issues: [description]" if problems discovered requiring fixes.
  </resume-signal>
</task>

</tasks>

<verification>
Human review of automated test results and spot-check of critical code changes.
</verification>

<success_criteria>
- [ ] Automated verification report generated with all test results
- [ ] All 9 issues show PASS status in verification report
- [ ] ShellCheck shows no SC2086 command injection warnings
- [ ] Python syntax validation passes for all 5 modified files
- [ ] Human verification confirms fixes are effective
- [ ] No regressions in existing workflow functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-05-SUMMARY.md`
</output>
