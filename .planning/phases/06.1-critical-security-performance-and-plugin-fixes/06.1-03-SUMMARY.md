---
phase: 06.1-critical-security-performance-and-plugin-fixes
plan: 03
subsystem: performance
tags: [python, mdanalysis, matplotlib, subprocess, memory, trajectory-analysis]

# Dependency graph
requires:
  - phase: 06-automation-infrastructure
    provides: Runner/executor baseline and complex-analysis stage plumbing
  - phase: 06.1-critical-security-performance-and-plugin-fixes
    provides: Ongoing critical security/performance hardening context
provides:
  - Contact-frequency frame striding with bounded sampled-frame counts
  - Guaranteed matplotlib figure cleanup in plotting helpers
  - Coordinate unit-range sanity warnings at analysis startup
  - File-streamed subprocess stdout/stderr capture in executor and runner paths
affects: [06.1-05-verification, phase-7-controlled-execution, long-run-stability]

# Tech tracking
tech-stack:
  added: []
  patterns: [bounded frame sampling, try-finally resource cleanup, streamed subprocess logging]

key-files:
  created:
    - .planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-03-SUMMARY.md
  modified:
    - scripts/com/3_com_ana_trj.py
    - scripts/infra/executor.py
    - scripts/agents/runner.py

key-decisions:
  - "Use ceiling-based stride math so sampled frames never exceed max_frames"
  - "Use try-finally cleanup for all matplotlib helper figures"
  - "Stream subprocess outputs to temporary files, then read back for return payload compatibility"

patterns-established:
  - "Pattern 1: Derive trajectory stride from user cap with bounded sampled-frame logging"
  - "Pattern 2: Initialize figure handle to None and always close in finally"
  - "Pattern 3: Replace capture_output buffering with file-handle streaming + best-effort cleanup"

# Metrics
duration: 7 min
completed: 2026-04-29
---

# Phase 6.1 Plan 03: Trajectory analysis performance and memory-leak hardening summary

**Trajectory contact analysis now samples bounded frame counts, plotting helpers guarantee figure cleanup on exceptions, coordinate ranges are sanity-checked for unit mismatches, and long-running subprocess logs are streamed via files to avoid memory blowups.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-29T07:47:15Z
- **Completed:** 2026-04-29T07:54:36Z
- **Tasks:** 4
- **Files modified:** 3

## Accomplishments
- Added frame striding to `compute_contact_frequency()` with bounded `max_frames` behavior and stride logging for large trajectories.
- Wrapped plotting helpers in `try/finally` to guarantee `plt.close(fig)` and eliminate exception-path figure leaks.
- Added non-blocking coordinate unit validation warnings in `analyze_trajectory()` startup flow.
- Replaced in-memory `capture_output=True` subprocess buffering with streamed temporary files in both executor and runner.

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix O(n×m) performance bottleneck with frame striding** - `c8e3371` (perf)
2. **Task 2: Fix matplotlib memory leaks with try-finally** - `e43e6ae` (fix)
3. **Task 3: Add unit conversion validation for coordinates** - `729cf51` (fix)
4. **Task 4: Fix subprocess buffer overflow by streaming to files** - `aa7ac1f` (fix)

**Plan metadata:** _(pending metadata commit)_

## Files Created/Modified
- `scripts/com/3_com_ana_trj.py` - Added contact frame striding, coordinate sanity warnings, and safe figure lifecycle handling.
- `scripts/infra/executor.py` - Switched local command execution output capture to streamed temp files.
- `scripts/agents/runner.py` - Switched runner script execution output capture to streamed temp files.
- `.planning/phases/06.1-critical-security-performance-and-plugin-fixes/06.1-03-SUMMARY.md` - Execution summary for this plan.

## Decisions Made
- Used ceiling-based stride computation to keep analyzed-frame count at or below `max_frames` instead of potentially exceeding it with floor division.
- Preserved existing return shapes for executor/runner while changing only subprocess capture mechanics to maintain backward compatibility.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected stride formula to enforce max_frames bound**
- **Found during:** Task 1 (Frame striding implementation)
- **Issue:** Floor-division stride can still analyze more than `max_frames` when trajectory length is not divisible by `max_frames`.
- **Fix:** Switched to ceiling-based stride math and logged computed sampled frame count.
- **Files modified:** `scripts/com/3_com_ana_trj.py`
- **Verification:** Synthetic harness confirmed 10,000-frame trajectory samples exactly 1,000 frames with `max_frames=1000`.
- **Committed in:** `c8e3371` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Fix tightens correctness of the plan’s frame-cap guarantee and improves performance predictability.

## Issues Encountered
- Direct full-script smoke run against bundled `expected/chm/BRD4/com/lig1` assets failed due to pre-existing topology/trajectory and selection-default incompatibilities unrelated to this plan’s code changes; behavior-level verification was completed with focused syntax checks plus a lightweight synthetic harness for stride/cleanup/unit-warning paths.

## User Setup Required
None - no external service configuration required.

## Authentication Gates
None.

## Next Phase Readiness
- All plan 06.1-03 must-have code patterns are implemented and verified.
- Ready for remaining Phase 6.1 plans and consolidated validation in 06.1-05.

---
*Phase: 06.1-critical-security-performance-and-plugin-fixes*
*Completed: 2026-04-29*
