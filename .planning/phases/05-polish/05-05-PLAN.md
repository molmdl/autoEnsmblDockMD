---
phase: 05-polish
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified: [docs/GUIDE.md]
autonomous: true

must_haves:
  truths:
    - "docs/GUIDE.md Part 1 covers configuration reference with all config.ini keys"
    - "Input preparation section explains what files user must provide and format requirements"
    - "Workspace setup section shows how to create and organize a workspace"
  artifacts:
    - path: "docs/GUIDE.md"
      provides: "Detailed human-facing usage guide (Part 1: setup and config)"
      contains: "config"
  key_links:
    - from: "docs/GUIDE.md"
      to: "scripts/config.ini.template"
      via: "config key documentation"
      pattern: "config"
---

<objective>
Create docs/GUIDE.md Part 1: configuration reference, input preparation, and workspace setup.

Purpose: Humans need a detailed guide beyond README overview. This is the human-facing counterpart to the 10 SKILL.md files agents get. Part 1 covers everything needed BEFORE running the pipeline.
Output: docs/GUIDE.md with config reference, input prep, and workspace setup (~150-200 lines).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@WORKFLOW.md
@scripts/config.ini.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ directory and GUIDE.md header + config reference</name>
  <files>docs/GUIDE.md</files>
  <action>
    Create docs/ directory if it doesn't exist. Create docs/GUIDE.md with:

    1. **Title** — "Detailed Usage Guide"
    2. **Table of Contents** — Link to all sections (Part 1 and Part 2 placeholders)
    3. **Configuration Reference** — Document ALL sections of config.ini.template:
       - Read scripts/config.ini.template fully
       - For each section/key: name, type, default, description, which stage uses it
       - Organize by config section: [general], [receptor], [docking], [complex], [md], [mmpbsa], [analysis]
       - Note which keys are Mode A vs Mode B specific
       - Use tables for compact presentation

    Target for config section: ~80-100 lines.
  </action>
  <verify>docs/GUIDE.md exists; grep -c "\[" docs/GUIDE.md shows config section headers</verify>
  <done>Configuration reference section is complete with all config.ini keys documented</done>
</task>

<task type="auto">
  <name>Task 2: Add input preparation and workspace setup sections</name>
  <files>docs/GUIDE.md</files>
  <action>
    Append to docs/GUIDE.md:

    4. **Input File Preparation** — What user must provide:
       - Receptor PDB: format requirements, hydrogen handling, chain naming
       - Reference ligand: coordinate file (.gro/.pdb) + topology (.itp), purpose (pocket definition)
       - Test ligands: coordinate + topology files, naming conventions
       - Force field files: which files, where to place them, AMBER vs CHARMM36m considerations
       - MDP files: what's needed (em.mdp, nvt.mdp, npt.mdp, md.mdp), where to find templates
       - Known pitfall: AMBER protein + CGenFF ligand causes crashes — must match FF families

    5. **Workspace Setup** — How to create and organize:
       - Directory structure diagram (work/{project}/ with stage subdirectories)
       - Copying input files to correct locations
       - Configuring config.ini from template
       - Verifying setup with status command

    Target for these sections: ~60-80 lines.
    Leave a "<!-- Part 2: Per-stage instructions will be added -->" placeholder at the end.
  </action>
  <verify>grep "Input File" docs/GUIDE.md && grep "Workspace" docs/GUIDE.md both return matches</verify>
  <done>docs/GUIDE.md Part 1 covers config reference, input preparation, and workspace setup</done>
</task>

</tasks>

<verification>
- docs/GUIDE.md exists with 150+ lines
- Configuration reference covers all config.ini.template sections
- Input preparation lists all required files with format requirements
- Workspace setup shows directory structure
- Force field pitfall documented
</verification>

<success_criteria>
A user can read docs/GUIDE.md Part 1 and know exactly how to configure the pipeline, what files to prepare, and how to set up their workspace — before running any stage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-05-SUMMARY.md`
</output>
