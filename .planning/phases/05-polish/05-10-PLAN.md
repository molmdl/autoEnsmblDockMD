---
phase: 05-polish
plan: 10
type: execute
wave: 3
depends_on: ["05-08", "05-09"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "All 10 SKILL.md files pass structural validation (YAML frontmatter + required sections)"
    - "All cross-references between docs resolve to existing files/sections"
    - "No orphaned or broken skill references in any documentation file"
  artifacts:
    - path: ".planning/phases/05-polish/05-10-validation-report.md"
      provides: "Structural validation results"
  key_links:
    - from: "AGENTS.md"
      to: ".opencode/skills/*/SKILL.md"
      via: "skill path references"
      pattern: "\\.opencode/skills/.*SKILL\\.md"
---

<objective>
Perform end-to-end structural validation of all documentation and skill files. Verify YAML frontmatter integrity, cross-reference consistency, and section completeness.

Purpose: Confirm the remediation is complete and all Phase 5 deliverables are in good shape.
Output: Validation report documenting pass/fail for each check.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-08-PLAN.md
@.planning/phases/05-polish/05-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate all 10 SKILL.md files structurally</name>
  <files>.planning/phases/05-polish/05-10-validation-report.md</files>
  <action>
    For each of the 10 SKILL.md files in .opencode/skills/:

    1. **YAML frontmatter check:** File starts with `---`, has closing `---`, contains required fields: name, description, license, compatibility, metadata (with author, version, agent, stage).

    2. **Required sections check:** File contains all of:
       - "When to use" (or "When to use this skill")
       - "Prerequisites"
       - "Usage"
       - "Parameters" (with a table containing CLI Flag and Default columns)
       - "Expected Output"
       - "Troubleshooting"

    3. **No 05-03 artifacts check:** File does NOT contain flat-format markers like "## Capability" as a top-level section header (without YAML context).

    Record results per skill in a validation report.
  </action>
  <verify>All 10 skills pass all 3 checks. Any failures documented with specific details.</verify>
  <done>Validation report created showing pass/fail for each skill file.</done>
</task>

<task type="auto">
  <name>Task 2: Validate cross-references across all docs</name>
  <files>.planning/phases/05-polish/05-10-validation-report.md</files>
  <action>
    1. Extract all file path references from: WORKFLOW.md, AGENTS.md, README.md, docs/GUIDE.md (if exists).
       - Look for patterns like `.opencode/skills/`, `scripts/commands/`, `scripts/`, `docs/`

    2. For each referenced path, verify the file exists on disk.

    3. Check AGENTS.md slash command table: each command maps to an existing script in scripts/commands/ and an existing SKILL.md.

    4. Check that skill `name` field in YAML frontmatter matches the directory name (e.g., .opencode/skills/rec-ensemble/SKILL.md has `name: rec-ensemble`).

    5. Append cross-reference results to the validation report.

    6. Commit the validation report: `docs(05-10): end-to-end structural validation report`
  </action>
  <verify>
    Validation report exists at .planning/phases/05-polish/05-10-validation-report.md with clear pass/fail per check.
  </verify>
  <done>Complete validation report showing all structural checks pass. Any failures documented with remediation notes.</done>
</task>

</tasks>

<verification>
1. Validation report exists and covers all 10 skills + all doc cross-references
2. All checks pass (or failures are documented with clear remediation path)
3. No 05-03 format artifacts remain anywhere
</verification>

<success_criteria>
- All 10 SKILL.md files pass YAML frontmatter + section checks
- All cross-references resolve to existing files
- Validation report committed to planning directory
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-10-SUMMARY.md`
</output>
