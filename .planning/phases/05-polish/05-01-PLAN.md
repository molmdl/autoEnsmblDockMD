---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [WORKFLOW.md]
autonomous: true

must_haves:
  truths:
    - "WORKFLOW.md documents every stage with script names, inputs, outputs"
    - "Both Mode A and Mode B workflows are fully described"
    - "User can follow WORKFLOW.md to run the entire pipeline manually"
  artifacts:
    - path: "WORKFLOW.md"
      provides: "Complete workflow reference with script mappings"
      contains: "scripts/"
  key_links:
    - from: "WORKFLOW.md"
      to: "scripts/"
      via: "script path references"
      pattern: "scripts/(rec|dock|com)/"
---

<objective>
Finalize WORKFLOW.md as the definitive workflow reference document.

Purpose: WORKFLOW.md is referenced by README, GUIDE, AGENTS.md, and skills. It must be complete and accurate before other docs are written.
Output: Complete WORKFLOW.md with all stages, scripts, inputs/outputs, and both modes documented.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@WORKFLOW.md
@scripts/config.ini.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit scripts and map to workflow stages</name>
  <files>WORKFLOW.md</files>
  <action>
    Scan all scripts in scripts/rec/, scripts/dock/, scripts/com/, scripts/infra/ to build a complete mapping:
    - Stage name → script path → required inputs → expected outputs
    - Execution order within each stage
    - Which scripts are Mode A only, Mode B only, or both

    Use `ls` and read script headers/usage to build the mapping. Do NOT read entire scripts — just headers and `-h` output where available.
  </action>
  <verify>Mapping covers all scripts in scripts/rec/, scripts/dock/, scripts/com/</verify>
  <done>Complete script→stage mapping exists as working notes for Task 2</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite WORKFLOW.md with complete stage reference</name>
  <files>WORKFLOW.md</files>
  <action>
    Rewrite WORKFLOW.md with this structure:
    1. **Overview** — What the pipeline does, two modes (A: reference pocket/AMBER, B: blind/CHARMM36m)
    2. **Prerequisites** — What user must provide (receptor PDB, ligands, force field files, MDP files)
    3. **Workspace Structure** — Expected directory layout
    4. **Stage Reference** — For each stage:
       - Stage name and purpose
       - Script(s) with paths
       - Required inputs (files, config keys)
       - Expected outputs
       - Mode-specific notes (A vs B differences)
    5. **Pipeline Flow Diagram** — ASCII diagram showing stage order and decision points

    Stages to document (in order):
    - Input preparation (user-provided files)
    - Receptor ensemble generation (pdb2pqr → pdb2gmx → MD → clustering → alignment)
    - Docking (ligand conversion → gnina ensemble docking → scoring → pose selection)
    - Complex setup (dock2com → topology assembly → solvation → ions → minimization)
    - MD simulation (equilibration → production)
    - MM/PBSA (trajectory processing → binding energy calculation)
    - Analysis (RMSD, RMSF, contacts, hydrogen bonds, fingerprints)

    Keep each stage section concise: 10-15 lines max. Reference config.ini.template keys where relevant.
    Total target: ~200-250 lines.
  </action>
  <verify>grep -c "scripts/" WORKFLOW.md should show 15+ script references; all 7 stages present</verify>
  <done>WORKFLOW.md is a complete, accurate reference for the entire pipeline</done>
</task>

</tasks>

<verification>
- WORKFLOW.md covers all 7 major stages
- Every script in scripts/rec/, scripts/dock/, scripts/com/ is referenced
- Both Mode A and Mode B are documented
- Inputs and outputs are listed for each stage
</verification>

<success_criteria>
A new user reading WORKFLOW.md can understand the complete pipeline, know which scripts to run in which order, and know what files each stage needs and produces.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
