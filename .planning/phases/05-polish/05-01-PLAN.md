---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [WORKFLOW.md]
autonomous: true

must_haves:
  truths:
    - "WORKFLOW.md documents every script in execution order for both Mode A and Mode B"
    - "Each stage lists exact commands, required inputs, and expected outputs"
    - "Configuration options (config.ini keys) referenced where relevant"
  artifacts:
    - path: "WORKFLOW.md"
      provides: "Complete workflow reference for humans and agents"
      contains: "scripts/rec/|scripts/dock/|scripts/com/"
  key_links:
    - from: "WORKFLOW.md"
      to: "scripts/"
      via: "script paths in workflow stages"
      pattern: "bash scripts/"
---

<objective>
Finalize WORKFLOW.md as the authoritative workflow reference documenting all scripts, their execution order, inputs/outputs, and configuration for both Mode A (reference pocket) and Mode B (blind docking).

Purpose: WORKFLOW.md is a blocker for Phase 5 — README and E2E validation depend on it.
Output: Complete, accurate WORKFLOW.md reflecting the actual scripts in `scripts/`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-CONTEXT.md
@WORKFLOW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit scripts against current WORKFLOW.md</name>
  <files>WORKFLOW.md</files>
  <action>
    Scan all scripts in `scripts/rec/`, `scripts/dock/`, `scripts/com/`, and `scripts/infra/` to build a complete inventory. Compare against the current WORKFLOW.md (198 lines) to identify:
    - Scripts mentioned but renamed/moved (e.g., old names vs current names)
    - Scripts that exist but are not documented
    - Missing input/output specifications
    - Missing configuration references (config.ini keys each script reads)

    List all discrepancies. Check script headers (`-h` output or usage comments) for canonical descriptions.
  </action>
  <verify>Print a table of all scripts with their documented status (documented/missing/outdated)</verify>
  <done>Complete inventory of scripts vs WORKFLOW.md coverage with all gaps identified</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite WORKFLOW.md with complete script reference</name>
  <files>WORKFLOW.md</files>
  <action>
    Rewrite WORKFLOW.md incorporating all findings from Task 1. Structure:

    1. **Header**: What this document covers, modes overview
    2. **Required Inputs**: Per-mode input file requirements (already good, refine)
    3. **Directory Structure**: Per-mode directory layout (already good, verify accuracy)
    4. **Workflow Stages** (per mode):
       - For each stage: purpose, exact command(s), required inputs, expected outputs, config keys used
       - Include the helper/utility scripts (config_loader, common.sh, setenv.sh)
    5. **Command Bridge**: Document the slash command → script mapping (from `scripts/commands/`)
    6. **Agent Skills**: Brief note that skills in `.opencode/skills/` provide agent-loadable versions
    7. **Helper Agents**: Keep existing checker/debugger section, expand slightly

    Preserve existing accurate content. Fix script names to match actual filenames. Add any undocumented scripts. Ensure every `scripts/` file appears somewhere.

    Do NOT add ligand preparation (SCRIPT-02 deferred). Keep concise — this is a reference, not a tutorial.
  </action>
  <verify>grep for every script filename in scripts/rec/, scripts/dock/, scripts/com/ and confirm each appears in WORKFLOW.md</verify>
  <done>WORKFLOW.md is complete, every script documented with correct paths, inputs, outputs, and execution order</done>
</task>

</tasks>

<verification>
- `grep -c 'scripts/' WORKFLOW.md` shows coverage of all script directories
- Every `.sh` and `.py` in `scripts/rec/`, `scripts/dock/`, `scripts/com/` appears in WORKFLOW.md
- Mode A and Mode B workflows both fully documented
</verification>

<success_criteria>
WORKFLOW.md is the single source of truth for "what scripts exist, what order to run them, what they need, what they produce" — accurate against the actual codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
