---
phase: 05-polish
plan: 12
type: execute
wave: 2
depends_on: ["05-11"]
files_modified:
  - .planning/phases/05-polish/05-12-e2e-test-report.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Every slash command script executes without syntax errors"
    - "Every skill file loads with valid YAML frontmatter"
    - "Command→script→skill chain is verified for all 10 commands"
    - "common.sh utilities work (source, help flags)"
  artifacts:
    - path: ".planning/phases/05-polish/05-12-e2e-test-report.md"
      provides: "E2E test results for all 10 commands"
      contains: "PASS"
  key_links:
    - from: "scripts/commands/*.sh"
      to: ".opencode/skills/*/SKILL.md"
      via: "command name → skill name mapping"
      pattern: "SKILL_NAME"
---

<objective>
End-to-end functional testing of all 10 slash commands and their skill chain.

Purpose: 05-10 validated structural correctness (files exist, cross-refs valid). This plan tests functional correctness — do the commands actually run? Do skills parse? Does the command→script→skill chain work?

Output: Test report documenting pass/fail for each command, plus human verification of results.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@AGENTS.md
@scripts/commands/common.sh
@.opencode/docs/AGENT-WORKFLOW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Functional test all 10 commands</name>
  <files>.planning/phases/05-polish/05-12-e2e-test-report.md</files>
  <action>
Run systematic functional tests on all 10 slash commands. For each command, test three levels:

**Level 1 — Script syntax check (all 10):**
```bash
source scripts/setenv.sh 2>/dev/null  # may fail if env not configured — OK
for cmd in scripts/commands/*.sh; do
  bash -n "$cmd" && echo "PASS: $cmd" || echo "FAIL: $cmd"
done
```

**Level 2 — Help flag (all 10, excluding common.sh):**
```bash
for cmd in scripts/commands/{rec-ensemble,dock-run,com-setup,com-md,com-mmpbsa,com-analyze,checker-validate,debugger-diagnose,orchestrator-resume,status}.sh; do
  bash "$cmd" --help 2>&1 | head -5
done
```
Record whether each produces usage text or errors.

**Level 3 — Skill YAML frontmatter parse (all 10):**
For each skill in `.opencode/skills/*/SKILL.md`:
- Extract YAML frontmatter (between first `---` markers)
- Verify required fields exist: `name`, `description`, `license`, `compatibility`, `metadata.agent`, `metadata.stage`
- Use `python3 -c "import yaml; ..."` or `grep` to validate

**Level 4 — Cross-reference chain (all 10):**
For each command script:
- Extract the skill name it references
- Verify matching SKILL.md exists
- Verify the SKILL.md references back to the script path

**Generate report** as `.planning/phases/05-polish/05-12-e2e-test-report.md` with:
- Table: Command | Syntax | Help | Skill Parse | Cross-ref | Status
- Any failures with error details
- Overall pass/fail count

If `source scripts/setenv.sh` fails (missing conda env, missing tools), note this but continue with what's testable. The goal is to test the script/skill infrastructure, not the computational tools themselves.
  </action>
  <verify>
- Report file exists at `.planning/phases/05-polish/05-12-e2e-test-report.md`
- All 10 commands tested at syntax level minimum
- All 10 skills tested for YAML frontmatter validity
- Report has clear pass/fail per command
  </verify>
  <done>All 10 commands pass syntax check. All 10 skills have valid YAML frontmatter. Cross-references verified. Report generated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
End-to-end functional test of all 10 slash commands and skill files. Test report generated with pass/fail status for syntax, help output, YAML parsing, and cross-references.
  </what-built>
  <how-to-verify>
1. Review the test report: `.planning/phases/05-polish/05-12-e2e-test-report.md`
2. Check that all 10 commands show PASS across all test levels
3. If any FAIL entries exist, decide whether they are acceptable (e.g., missing conda env for help flag) or need fixing
4. Optionally run one or more commands yourself:
   ```bash
   source scripts/setenv.sh
   bash scripts/commands/status.sh --help
   bash scripts/commands/rec-ensemble.sh --help
   ```
5. If you want to test the full skill loading chain, invoke a skill in OpenCode:
   - Type `/status` to test the status skill loads correctly
   - Type `/rec-ensemble --help` to test a runner skill
  </how-to-verify>
  <resume-signal>Type "approved" if test results are acceptable, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Test report covers all 10 commands × 4 test levels
- No syntax errors in any command script
- All YAML frontmatter parses correctly
- Cross-reference chain intact for all 10 commands
</verification>

<success_criteria>
- All 10 slash commands pass functional testing
- All 10 skill files have valid, parseable YAML frontmatter
- Human approves test results
- E2E validation deliverable from 05-CONTEXT.md is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-12-SUMMARY.md`
</output>
