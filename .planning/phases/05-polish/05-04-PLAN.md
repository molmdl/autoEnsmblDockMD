---
phase: 05-polish
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Full pipeline executes from receptor prep through MMPBSA without crashing"
    - "Output structure matches expected directory layout documented in WORKFLOW.md"
    - "Results are structurally valid (files exist, non-empty, correct format)"
  artifacts:
    - path: ".planning/phases/05-polish/05-E2E-REPORT.md"
      provides: "End-to-end validation report"
      contains: "PASS|FAIL|WARNING"
  key_links:
    - from: "WORKFLOW.md"
      to: "work/input"
      via: "test data used for validation"
      pattern: "work/input"
---

<objective>
Perform end-to-end validation of the complete pipeline using existing test data from `work/input`, verifying that scripts execute correctly and produce expected outputs.

Purpose: Validate that the documented workflow actually works before release.
Output: E2E validation report in `.planning/phases/05-polish/05-E2E-REPORT.md`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@WORKFLOW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate pipeline scripts and structure</name>
  <files>.planning/phases/05-polish/05-E2E-REPORT.md</files>
  <action>
    Perform structural validation of the pipeline WITHOUT running heavy computations (no MD, no docking — those require GPU/HPC time). Instead:

    1. **Script existence check**: Verify every script referenced in WORKFLOW.md exists and is executable
    2. **Help flag check**: Run `bash scripts/SCRIPT.sh -h` for each script and confirm it responds with usage info
    3. **Config template check**: Verify `scripts/config.ini.template` exists and contains expected keys
    4. **Input data check**: Verify `work/input/` contains the expected directory structure and input files
    5. **Slash command check**: Verify all scripts in `scripts/commands/` are executable and source common.sh
    6. **Skill file check**: Verify all 10 SKILL.md files in `.opencode/skills/` are parseable (contain required metadata)
    7. **Environment check**: Source `scripts/setenv.sh` and verify GROMACS, gnina, Python are accessible
    8. **Cross-reference check**: Verify WORKFLOW.md script names match README.md script names match actual files

    Write results to `.planning/phases/05-polish/05-E2E-REPORT.md` with PASS/FAIL/WARNING for each check.

    Per CONTEXT.md: success criteria is "structure correct + values acceptable" not exact match.
  </action>
  <verify>cat .planning/phases/05-polish/05-E2E-REPORT.md and confirm all categories checked</verify>
  <done>E2E report exists with PASS/FAIL/WARNING for all structural validation checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Structural validation of the complete pipeline — scripts existence, help flags, config, inputs, commands, skills, environment, and cross-references checked</what-built>
  <how-to-verify>
    1. Review `.planning/phases/05-polish/05-E2E-REPORT.md` for any FAIL or WARNING items
    2. If you want to run a full pipeline test (receptor prep → docking → MD → MMPBSA), do so manually in a test workspace — this validates the compute path
    3. Spot-check README.md and WORKFLOW.md for accuracy
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- 05-E2E-REPORT.md exists with structured PASS/FAIL/WARNING results
- No critical FAIL items remain unaddressed
- Human has reviewed and approved
</verification>

<success_criteria>
All structural validation checks pass. Human has reviewed docs and validation report. Phase 5 deliverables are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-04-SUMMARY.md`
</output>
