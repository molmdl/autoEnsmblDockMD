---
phase: 05-polish
plan: 09
type: execute
wave: 2
depends_on: ["05-08"]
files_modified:
  - WORKFLOW.md
  - AGENTS.md
  - README.md
  - docs/GUIDE.md
autonomous: true

must_haves:
  truths:
    - "WORKFLOW.md references to skills are compatible with restored YAML frontmatter format"
    - "AGENTS.md skill cross-references match actual skill names in frontmatter"
    - "README.md and GUIDE.md do not reference 05-03's flat markdown skill format"
  artifacts:
    - path: "WORKFLOW.md"
      provides: "Workflow reference compatible with restored skills"
    - path: "AGENTS.md"
      provides: "Agent overview with correct skill cross-references"
  key_links:
    - from: "AGENTS.md"
      to: ".opencode/skills/*/SKILL.md"
      via: "skill file path references"
      pattern: "\\.opencode/skills/"
---

<objective>
Review docs produced by plans 05-01, 05-02, 05-04, 05-05, 05-06 for compatibility with restored SKILL.md format. Fix any cross-references that assumed 05-03's flat markdown format.

Purpose: Ensure all surviving documentation is consistent with the restored YAML frontmatter skill format.
Output: Any incompatible references fixed in WORKFLOW.md, AGENTS.md, README.md, docs/GUIDE.md.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-08-PLAN.md
@WORKFLOW.md
@AGENTS.md
@README.md
@docs/GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit WORKFLOW.md and AGENTS.md for skill format references</name>
  <files>WORKFLOW.md, AGENTS.md</files>
  <action>
    1. Read WORKFLOW.md and search for any references to skill file structure, skill sections, or skill metadata. Check that references are compatible with YAML frontmatter format (not flat markdown).

    2. Read AGENTS.md and verify:
       - Skill file path references point to correct locations (.opencode/skills/{name}/SKILL.md)
       - Any description of skill format matches YAML frontmatter (name, description, license, compatibility, metadata fields)
       - Agent-to-skill mappings are correct

    3. Fix any references that assumed 05-03's format (e.g., references to "Capability" section instead of "When to use", or missing YAML frontmatter mentions).

    4. If no changes needed, document that explicitly — do NOT make gratuitous edits.
  </action>
  <verify>
    Run: `grep -n "Capability\|Success Criteria" WORKFLOW.md AGENTS.md` — should return zero matches (these were 05-03 section names).
    Run: `grep -c "SKILL.md" AGENTS.md` — should show cross-references exist.
  </verify>
  <done>WORKFLOW.md and AGENTS.md are compatible with restored YAML frontmatter skill format. Any 05-03-style references fixed or confirmed absent.</done>
</task>

<task type="auto">
  <name>Task 2: Audit README.md and docs/GUIDE.md for skill format references</name>
  <files>README.md, docs/GUIDE.md</files>
  <action>
    1. Read README.md and search for any references to skill files, skill format, or skill sections. Verify compatibility with YAML frontmatter format.

    2. Read docs/GUIDE.md and do the same check — look for any description of skill structure that matches the broken flat format.

    3. Fix any incompatible references. If docs describe skill structure, ensure they mention YAML frontmatter with name/description/license/compatibility/metadata fields.

    4. If no changes needed, document that explicitly.

    5. Commit any fixes: `fix(05): align doc cross-references with restored skill format`
  </action>
  <verify>
    Run: `grep -n "Capability\|Success Criteria" README.md docs/GUIDE.md 2>/dev/null` — should return zero matches for 05-03-style sections.
  </verify>
  <done>All project documentation is consistent with restored YAML frontmatter skill format.</done>
</task>

</tasks>

<verification>
1. No document references 05-03's flat markdown skill format
2. All skill path references are correct
3. Agent-to-skill mappings in AGENTS.md match actual skill names
</verification>

<success_criteria>
- Zero references to 05-03's "Capability", "Success Criteria" skill sections in any doc
- All skill cross-references use correct paths
- Documents either unchanged (already compatible) or fixed
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-09-SUMMARY.md`
</output>
