---
phase: 05-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [.opencode/skills/rec-ensemble/SKILL.md, .opencode/skills/dock-run/SKILL.md, .opencode/skills/com-setup/SKILL.md, .opencode/skills/com-md/SKILL.md, .opencode/skills/com-mmpbsa/SKILL.md, .opencode/skills/com-analyze/SKILL.md, .opencode/skills/checker-validate/SKILL.md, .opencode/skills/debugger-diagnose/SKILL.md, .opencode/skills/orchestrator-resume/SKILL.md, .opencode/skills/status/SKILL.md]
autonomous: true

must_haves:
  truths:
    - "All 10 SKILL.md files have consistent metadata fields"
    - "Each skill has: capability summary, parameters, success criteria, usage example"
    - "Skill parameters reference config.ini.template keys where applicable"
  artifacts:
    - path: ".opencode/skills/rec-ensemble/SKILL.md"
      provides: "Receptor ensemble skill with standardized metadata"
      contains: "parameters"
    - path: ".opencode/skills/dock-run/SKILL.md"
      provides: "Docking skill with standardized metadata"
      contains: "parameters"
  key_links:
    - from: ".opencode/skills/*/SKILL.md"
      to: "scripts/config.ini.template"
      via: "parameter key references"
      pattern: "config"
---

<objective>
Audit and standardize all 10 SKILL.md files to ensure consistent metadata format.

Purpose: Skills are the detailed agent documentation. They must have consistent structure so agents can parse them reliably. This is the agent-facing counterpart to docs/GUIDE.md for humans.
Output: 10 standardized SKILL.md files with consistent metadata fields.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@scripts/config.ini.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define standard skill metadata template</name>
  <files>.opencode/skills/rec-ensemble/SKILL.md</files>
  <action>
    Read all 10 SKILL.md files (just first 15 lines each) to identify current inconsistencies.
    Define the standard metadata template that every skill must follow:

    ```
    # Skill: {name}
    **Stage:** {WorkflowStage value}
    **Agent:** {runner|checker|debugger|orchestrator}

    ## Capability
    {1-2 sentence summary}

    ## Parameters
    | Parameter | Config Key | Required | Description |
    |-----------|-----------|----------|-------------|

    ## Scripts
    | Script | Purpose |
    |--------|---------|

    ## Success Criteria
    - {measurable criterion 1}
    - {measurable criterion 2}

    ## Usage Example
    {slash command or agent invocation}

    ## Workflow
    {step-by-step instructions for agent}
    ```

    Apply this template to rec-ensemble/SKILL.md first as the reference implementation.
  </action>
  <verify>Read rec-ensemble/SKILL.md and confirm all metadata sections present</verify>
  <done>Standard template defined and applied to rec-ensemble/SKILL.md</done>
</task>

<task type="auto">
  <name>Task 2: Apply standard template to remaining 9 SKILL.md files</name>
  <files>.opencode/skills/dock-run/SKILL.md, .opencode/skills/com-setup/SKILL.md, .opencode/skills/com-md/SKILL.md, .opencode/skills/com-mmpbsa/SKILL.md, .opencode/skills/com-analyze/SKILL.md, .opencode/skills/checker-validate/SKILL.md, .opencode/skills/debugger-diagnose/SKILL.md, .opencode/skills/orchestrator-resume/SKILL.md, .opencode/skills/status/SKILL.md</files>
  <action>
    For each of the remaining 9 SKILL.md files:
    1. Read the current content
    2. Restructure to match the standard template from Task 1
    3. Preserve all existing workflow/instruction content — only restructure metadata sections
    4. Ensure Parameters table references config.ini.template keys where applicable
    5. Add any missing sections (Success Criteria, Usage Example)

    Process files in batches of 3 to stay within context. Do NOT read all 9 at once.
    Batch 1: dock-run, com-setup, com-md
    Batch 2: com-mmpbsa, com-analyze, checker-validate
    Batch 3: debugger-diagnose, orchestrator-resume, status
  </action>
  <verify>For each SKILL.md: grep for "## Capability", "## Parameters", "## Success Criteria", "## Usage Example" — all 4 must be present in all 10 files</verify>
  <done>All 10 SKILL.md files follow the standard metadata template</done>
</task>

</tasks>

<verification>
- All 10 SKILL.md files have identical section structure
- Parameters tables reference config.ini.template keys
- No content lost from existing workflow instructions
- Consistent formatting across all files
</verification>

<success_criteria>
Any agent loading any SKILL.md file can parse it with the same logic — consistent sections, consistent metadata fields, consistent format.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-03-SUMMARY.md`
</output>
