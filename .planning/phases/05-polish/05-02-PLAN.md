---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [AGENTS.md, .opencode/skills/*/SKILL.md]
autonomous: true

must_haves:
  truths:
    - "AGENTS.md organized by agent type with clear responsibilities and cross-references to skills"
    - "Agent docs (DOC-02) in .opencode/skills/ have consistent metadata: capability summary, parameters, success criteria"
    - "Experimental status clearly marked in AGENTS.md"
  artifacts:
    - path: "AGENTS.md"
      provides: "Human-readable agent overview and getting-started guide"
      contains: "orchestrator|runner|analyzer|checker|debugger"
    - path: ".opencode/skills/rec-ensemble/SKILL.md"
      provides: "Example of agent-loadable skill with metadata"
      contains: "parameters|success_criteria"
  key_links:
    - from: "AGENTS.md"
      to: ".opencode/skills/"
      via: "cross-references to skill files"
      pattern: "\\.opencode/skills/"
---

<objective>
Restructure AGENTS.md as a human-readable agent overview organized by agent type, and audit/enhance all skill files in `.opencode/skills/` for consistent agent-loadable metadata (DOC-02, DOC-04).

Purpose: AGENTS.md becomes the "getting started for agent developers" guide. Skill files become the runtime-loadable detailed docs.
Output: Restructured AGENTS.md + consistent skill file metadata across all 10 skills.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure AGENTS.md</name>
  <files>AGENTS.md</files>
  <action>
    Rewrite AGENTS.md (currently 81 lines, mostly project guidelines). New structure per CONTEXT.md decisions:

    1. **Header**: What this file is, experimental status banner
    2. **Agent Types**: For each of the 5 agent types (orchestrator, runner, analyzer, checker, debugger):
       - Role & responsibilities (2-3 sentences)
       - Skills it loads (cross-reference to `.opencode/skills/SKILL_NAME/SKILL.md`)
       - File-based handoff: what it reads, what it writes
    3. **Workflow**: How agents interact (orchestrator spawns → runner executes → checker validates → debugger fixes)
    4. **Slash Commands**: Table mapping commands to agent dispatching (from `scripts/commands/`)
    5. **Skill Authoring Guide**: Brief guide for adding new skills (format, location, required fields)
    6. **Constraints**: Preserve existing important constraints (no rm, no sudo, conda only, config-driven, etc.)
    7. **Environment Setup**: Keep setenv.sh reference, conda env reference

    Tone: Professional, concise, structured for quick scanning. Mark experimental clearly.
    Keep the essential content from current AGENTS.md (constraints, environment, workflow reference) but reorganize.
  </action>
  <verify>grep for all 5 agent types and all 10 skill names in AGENTS.md</verify>
  <done>AGENTS.md organized by agent type with cross-references to skills, experimental status marked</done>
</task>

<task type="auto">
  <name>Task 2: Audit and enhance skill file metadata (DOC-02)</name>
  <files>.opencode/skills/*/SKILL.md</files>
  <action>
    Audit all 10 skill files in `.opencode/skills/` for consistent metadata. Each SKILL.md should have (per CONTEXT.md: YAML-optimized with Markdown readability):

    Required fields (add if missing, standardize if inconsistent):
    - `capability_summary`: 1-2 sentence description
    - `agent_type`: which agent loads this skill
    - `parameters`: key config.ini keys or CLI flags this skill uses
    - `success_criteria`: how to verify the stage completed correctly
    - `scripts_used`: list of scripts this skill invokes
    - `usage_example`: one concrete example command/invocation

    Skills to audit: checker-validate, com-analyze, com-md, com-mmpbsa, com-setup, debugger-diagnose, dock-run, orchestrator-resume, rec-ensemble, status.

    Read each SKILL.md, identify missing fields, add them. Do NOT rewrite existing good content — only add missing metadata sections.

    Important: Read skills one at a time to avoid context overload. Start with one, establish the pattern, then apply to remaining.
  </action>
  <verify>For each of the 10 SKILL.md files, verify presence of: capability_summary, agent_type, parameters, success_criteria</verify>
  <done>All 10 skill files have consistent metadata fields, parseable by agents at runtime</done>
</task>

</tasks>

<verification>
- AGENTS.md mentions all 5 agent types with skill cross-references
- All 10 SKILL.md files contain the 4 required metadata fields
- Experimental status clearly visible in AGENTS.md
</verification>

<success_criteria>
AGENTS.md serves as human-readable overview; skill files serve as agent-loadable runtime docs. Both are consistent and cross-referenced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-02-SUMMARY.md`
</output>
