---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [AGENTS.md]
autonomous: true

must_haves:
  truths:
    - "AGENTS.md is organized by agent type with clear responsibilities"
    - "Each agent type cross-references its skill file(s)"
    - "Experimental status is prominently marked"
    - "File-based handoff patterns are documented"
  artifacts:
    - path: "AGENTS.md"
      provides: "Agent overview with cross-references to skills"
      contains: "opencode/skills/"
  key_links:
    - from: "AGENTS.md"
      to: ".opencode/skills/*/SKILL.md"
      via: "cross-reference paths"
      pattern: "\\.opencode/skills/"
---

<objective>
Restructure AGENTS.md as a human-readable agent overview with cross-references to distributed skill files.

Purpose: AGENTS.md currently mixes project requirements with agent guidelines. Restructure it as the agent overview document — organized by agent type, with cross-references to SKILL.md files for implementation details.
Output: Clean, restructured AGENTS.md (~120-150 lines).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure AGENTS.md by agent type</name>
  <files>AGENTS.md</files>
  <action>
    Rewrite AGENTS.md with this structure:

    1. **Header** — Title, one-line description, EXPERIMENTAL badge/warning
    2. **Overview** — What the agent system does, how it relates to scripts (agents call scripts, not replace them)
    3. **Agent Types** — One section per agent type:
       - **Orchestrator** — Manages workflow state, spawns agents, handles checkpoints. Skills: `orchestrator-resume`, `status`
       - **Runner** — Executes stage scripts via slash commands. Skills: `rec-ensemble`, `dock-run`, `com-setup`, `com-md`, `com-mmpbsa`, `com-analyze`
       - **Checker** — Validates results against thresholds. Skills: `checker-validate`
       - **Debugger** — Diagnoses failures with version-aware context. Skills: `debugger-diagnose`
       Each agent section: 5-10 lines max, with skill file path references.
    4. **File-Based Handoff** — How agents pass state via HandoffRecord JSON, checkpoint files, workspace artifacts
    5. **Slash Commands** — Table mapping commands to scripts (reference scripts/commands/)
    6. **Constraints** — Safety rules (no rm, no sudo, env sourcing, config-driven)
    7. **Prerequisites** — Environment setup, conda, GROMACS version

    Preserve all safety rules from current AGENTS.md (no rm, no sudo, etc.).
    Remove the "Detailed requirements" section (those are in PROJECT.md).
    Remove "V. Scripts" section (covered by WORKFLOW.md).
    Target: ~120-150 lines.
  </action>
  <verify>grep -c "SKILL.md" AGENTS.md should show 8+ cross-references; grep "EXPERIMENTAL" AGENTS.md returns match</verify>
  <done>AGENTS.md is organized by agent type with skill cross-references and experimental marking</done>
</task>

</tasks>

<verification>
- AGENTS.md has sections for each agent type
- Each agent type references its SKILL.md file(s)
- Experimental status is clearly marked
- Safety constraints are preserved
- No duplication of content from WORKFLOW.md or PROJECT.md
</verification>

<success_criteria>
An agent loading AGENTS.md can identify its type, find its skill file(s), understand handoff patterns, and know the safety constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-02-SUMMARY.md`
</output>
