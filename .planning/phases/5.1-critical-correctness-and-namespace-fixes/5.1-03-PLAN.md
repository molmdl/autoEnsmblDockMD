---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dock/4_dock2com.sh
  - scripts/dock/4_dock2com_ref.sh
  - scripts/dock/4_dock2com_2.2.1.py
autonomous: true

must_haves:
  truths:
    - "dock2com wrappers create com.gro/complex.gro before requiring it"
    - "Ligand GRO atom order is validated against ITP before posre_lig.itp generation"
  artifacts:
    - path: "scripts/dock/4_dock2com.sh"
      provides: "Complex GRO creation and validation"
    - path: "scripts/dock/4_dock2com_ref.sh"
      provides: "Reference complex GRO creation and validation"
    - path: "scripts/dock/4_dock2com_2.2.1.py"
      provides: "Atom-order validation for position restraints"
  key_links:
    - from: "scripts/dock/4_dock2com.sh"
      to: "com.gro"
      via: "coordinate merge step"
      pattern: "com\\.gro\\|complex\\.gro"
---

<objective>
Fix dock-to-complex artifact contract so wrappers create `com.gro` before requiring it, and add ligand GRO↔ITP atom-order validation before position restraint generation.

Purpose: Without com.gro creation, dock2com fails. Without atom-order validation, position restraints can bind wrong atoms.
Output: Working dock2com pipeline with safety validation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/code_analysis/2026-04-19-critical-review.md
@scripts/dock/4_dock2com.sh
@scripts/dock/4_dock2com_ref.sh
@scripts/dock/4_dock2com_2.2.1.py
@scripts/dock/4_dock2com_1.py
@scripts/dock/4_dock2com_2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix com.gro creation in dock2com wrappers</name>
  <files>scripts/dock/4_dock2com.sh, scripts/dock/4_dock2com_ref.sh</files>
  <action>
    In both `4_dock2com.sh` (around lines 245-266) and `4_dock2com_ref.sh` (around lines 267-288):

    1. After the SDF→GRO conversion and topology assembly steps complete, ADD a step that creates `com.gro` (or `complex.gro`) by merging the receptor GRO and ligand GRO files.
    2. The merge should: concatenate receptor and ligand coordinates, update total atom count in the GRO header, preserve box vectors from the receptor GRO.
    3. This step must occur BEFORE the `require_file "${com_gro}"` validation.
    4. If merge fails, emit a clear error and exit.

    Reference pattern: Look at how `expected/chm/BRD4/scripts/dock/` handles complex coordinate assembly if available, or use standard GRO merge logic (header line, atom count = receptor_atoms + ligand_atoms, receptor coords, ligand coords, box line).
  </action>
  <verify>
    1. `grep -n "com\.gro\|complex\.gro\|merge\|cat.*gro" scripts/dock/4_dock2com.sh` shows GRO creation before require_file
    2. `grep -n "com\.gro\|complex\.gro\|merge\|cat.*gro" scripts/dock/4_dock2com_ref.sh` shows same
    3. `bash -n scripts/dock/4_dock2com.sh && bash -n scripts/dock/4_dock2com_ref.sh` passes
  </verify>
  <done>
    - com.gro is created by merging receptor + ligand GRO files before validation
    - Both wrapper scripts pass syntax check
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ligand GRO↔ITP atom-order validation</name>
  <files>scripts/dock/4_dock2com_2.2.1.py</files>
  <action>
    In `4_dock2com_2.2.1.py` (63 lines, generates posre_lig.itp):

    1. Before generating position restraints, add validation that compares:
       - Atom count in the ligand GRO file
       - Atom count in the ligand ITP file (from `[ atoms ]` section)
    2. If counts differ, print an error message and exit with non-zero status.
    3. Optionally, compare atom names between GRO and ITP to detect ordering mismatches (warn if names differ at same index).
    4. Only proceed with posre_lig.itp generation if validation passes.

    This is a safety check — if SDF→GRO conversion produces different atom ordering than the ITP, position restraints will bind wrong atoms.
  </action>
  <verify>
    1. `python3 -c "import ast; ast.parse(open('scripts/dock/4_dock2com_2.2.1.py').read())"` passes
    2. `grep -n "atom.*count\|validate\|mismatch\|ITP" scripts/dock/4_dock2com_2.2.1.py` shows validation logic
  </verify>
  <done>
    - Atom count validation between GRO and ITP before posre_lig.itp generation
    - Clear error on mismatch
    - Script passes Python syntax check
  </done>
</task>

</tasks>

<verification>
- All three files pass syntax checks
- com.gro creation precedes require_file validation
- Atom-order validation prevents silent restraint errors
</verification>

<success_criteria>
- dock2com wrappers self-consistently create and validate com.gro
- Position restraint generation includes atom-order safety check
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-03-SUMMARY.md`
</output>
