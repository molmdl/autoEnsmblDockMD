---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_pipeline.sh
  - scripts/com/3_com_ana_trj.py
autonomous: true

must_haves:
  truths:
    - "run_pipeline.sh forwards --ligand (not --target) when dispatching com_ana stage"
    - "RMSD calculation in 3_com_ana_trj.py uses alignment before measuring displacement"
  artifacts:
    - path: "scripts/run_pipeline.sh"
      provides: "Correct argument forwarding for all stages"
    - path: "scripts/com/3_com_ana_trj.py"
      provides: "Alignment-corrected RMSD timeseries"
  key_links:
    - from: "scripts/run_pipeline.sh"
      to: "scripts/com/3_ana.sh"
      via: "--ligand flag"
      pattern: "ligand"
---

<objective>
Fix run_pipeline.sh argument forwarding for com_ana stage and correct RMSD alignment in 3_com_ana_trj.py.

Purpose: Pipeline cannot target single ligand for analysis; RMSD values overstate motion without alignment.
Output: Corrected pipeline dispatch and scientifically correct RMSD.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/code_analysis/2026-04-19-critical-review.md
@scripts/run_pipeline.sh
@scripts/com/3_ana.sh
@scripts/com/3_com_ana_trj.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix run_pipeline.sh argument forwarding for com_ana</name>
  <files>scripts/run_pipeline.sh</files>
  <action>
    Fix the argument forwarding mismatch (around lines 82-84, 215-228):

    1. The `com_ana` stage is marked as `target` mode and forwards `--target`, but `scripts/com/3_ana.sh` only accepts `--ligand`.
    2. Change the forwarding for `com_ana` to use `--ligand` instead of `--target`.
    3. Review all other stage dispatches to confirm they forward the correct flag names matching their callee's CLI interface.
    4. If the stage mode classification (target vs ligand) drives the flag name, either fix the mode for com_ana or add special-case handling.

    IMPORTANT: Only fix the argument name mapping. Do not change the overall dispatch architecture.
  </action>
  <verify>
    1. `grep -n "com_ana\|--target\|--ligand" scripts/run_pipeline.sh` shows --ligand used for com_ana dispatch
    2. `bash -n scripts/run_pipeline.sh` passes syntax check
  </verify>
  <done>
    - com_ana stage forwards --ligand to 3_ana.sh
    - All stage dispatches use correct argument names for their callees
  </done>
</task>

<task type="auto">
  <name>Task 2: Add alignment correction to RMSD in 3_com_ana_trj.py</name>
  <files>scripts/com/3_com_ana_trj.py</files>
  <action>
    Fix the `compute_rmsd()` function (around lines 90-105):

    1. Currently measures raw coordinate displacement from first frame without superposition.
    2. Add structural alignment (superposition) before RMSD calculation. Use MDAnalysis `align.alignto()` or `rms.rmsd()` with superposition=True.
    3. Align each frame to the first frame (or reference frame) on the protein backbone before computing ligand RMSD.
    4. This ensures RMSD values reflect genuine structural change, not translational/rotational drift.

    Reference: `expected/chm/BRD4/scripts/com/com_ana_trj.py` if available for the correct pattern.

    IMPORTANT: Preserve existing output format (rmsd_timeseries.csv). Only change the RMSD calculation method.
  </action>
  <verify>
    1. `python3 -c "import ast; ast.parse(open('scripts/com/3_com_ana_trj.py').read())"` passes
    2. `grep -n "align\|superpos\|RMSD" scripts/com/3_com_ana_trj.py` shows alignment before RMSD
  </verify>
  <done>
    - RMSD calculation includes structural alignment/superposition
    - Output format unchanged (rmsd_timeseries.csv)
    - Script passes Python syntax check
  </done>
</task>

</tasks>

<verification>
- run_pipeline.sh syntax valid, com_ana uses --ligand
- 3_com_ana_trj.py syntax valid, RMSD uses alignment
</verification>

<success_criteria>
- Pipeline can target single ligand for com_ana stage
- RMSD timeseries reflects alignment-corrected values
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-04-SUMMARY.md`
</output>
