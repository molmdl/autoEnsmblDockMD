---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/com/0_prep.sh
autonomous: true

must_haves:
  truths:
    - "sys.top includes receptor moleculetype definition from receptor topology"
    - "gmx solvate/grompp succeeds with generated sys.top"
    - "water_model config value is used in gmx solvate instead of hardcoded spc216"
  artifacts:
    - path: "scripts/com/0_prep.sh"
      provides: "Correct topology assembly and water model usage"
      contains: "RECEPTOR_TOP"
  key_links:
    - from: "scripts/com/0_prep.sh"
      to: "sys.top"
      via: "build_sys_top() function"
      pattern: "cat.*receptor.*top"
---

<objective>
Fix `scripts/com/0_prep.sh` topology generation so that `sys.top` includes the receptor moleculetype definition from the receptor topology file, and use the configured `water_model` instead of hardcoded `spc216`.

Purpose: Without receptor topology content in sys.top, downstream MD simulations fail or produce scientifically invalid results.
Output: Corrected `0_prep.sh` with proper topology assembly and water model usage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/code_analysis/2026-04-19-critical-review.md
@scripts/com/0_prep.sh
@expected/chm/BRD4/scripts/com/0_prep.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build_sys_top() to include receptor topology content</name>
  <files>scripts/com/0_prep.sh</files>
  <action>
    Fix the `build_sys_top()` function (around lines 265-335) to include receptor topology content in `sys.top`.

    Current bug: `build_sys_top()` writes only the force-field `#include` and ligand ITP `#include`, then declares `[ molecules ] Protein 1` — but never includes the receptor moleculetype definition.

    Required fix:
    1. Read the reference implementation at `expected/chm/BRD4/scripts/com/0_prep.sh` to understand the correct topology assembly pattern.
    2. After the force-field include line, extract and insert the receptor moleculetype content from `RECEPTOR_TOP` (the receptor .top file). This typically means copying everything between the force-field include and the `[ molecules ]` section from the receptor topology.
    3. Ensure the ligand ITP include comes after receptor content.
    4. Ensure `[ molecules ]` section lists both Protein and ligand entries.

    Also fix water_model usage (around line 327-330):
    - Replace hardcoded `-cs spc216` in `gmx solvate` with `-cs ${water_model}` using the value read from config.
    - If `water_model` is empty/unset, default to `spc216` for backward compatibility.

    IMPORTANT: Preserve all existing error handling, logging, and CLI interface. Only modify topology assembly and solvation logic.
  </action>
  <verify>
    1. `grep -n "RECEPTOR_TOP\|receptor.*top\|cat.*top" scripts/com/0_prep.sh` shows receptor topology content being incorporated into sys.top
    2. `grep -n "water_model\|spc216" scripts/com/0_prep.sh` shows water_model variable used in gmx solvate command
    3. `bash -n scripts/com/0_prep.sh` passes syntax check
  </verify>
  <done>
    - `build_sys_top()` includes receptor moleculetype definition from RECEPTOR_TOP in generated sys.top
    - `gmx solvate` uses configured water_model instead of hardcoded spc216
    - Script passes bash syntax check
  </done>
</task>

</tasks>

<verification>
- `bash -n scripts/com/0_prep.sh` — no syntax errors
- Topology assembly logic includes receptor content (grep confirms)
- Water model is config-driven with spc216 fallback
</verification>

<success_criteria>
- sys.top generation includes receptor moleculetype definition
- water_model config value drives solvation
- No regressions in existing 0_prep.sh functionality
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-01-SUMMARY.md`
</output>
