---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/com/2_mmpbsa.sh
  - scripts/com/2_trj4mmpbsa.sh
autonomous: true

must_haves:
  truths:
    - "MM/PBSA group IDs are derived from the actual index file, not hardcoded"
    - "Group selection adapts correctly when index ordering changes across systems"
  artifacts:
    - path: "scripts/com/2_mmpbsa.sh"
      provides: "Dynamic group-ID resolution for MM/PBSA"
    - path: "scripts/com/2_trj4mmpbsa.sh"
      provides: "Index creation with propagated group IDs"
  key_links:
    - from: "scripts/com/2_trj4mmpbsa.sh"
      to: "scripts/com/2_mmpbsa.sh"
      via: "index.ndx group IDs"
      pattern: "group.*id\\|cg.*receptor\\|cg.*ligand"
---

<objective>
Fix MM/PBSA group-ID propagation so that `2_mmpbsa.sh` uses dynamically determined group IDs from the index file created by `2_trj4mmpbsa.sh`, instead of hardcoded values.

Purpose: Hardcoded group IDs (1 and 12) cause silent science errors when index ordering differs across systems.
Output: Corrected MM/PBSA scripts with dynamic group resolution.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/code_analysis/2026-04-19-critical-review.md
@scripts/com/2_mmpbsa.sh
@scripts/com/2_trj4mmpbsa.sh
@expected/chm/BRD4/scripts/com/mmpbsa.sh
@expected/chm/BRD4/scripts/com/trj4mmpbsa.sh
@scripts/config.ini.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix group-ID propagation between trj4mmpbsa and mmpbsa</name>
  <files>scripts/com/2_trj4mmpbsa.sh, scripts/com/2_mmpbsa.sh</files>
  <action>
    Reference the working implementations at `expected/chm/BRD4/scripts/com/trj4mmpbsa.sh` and `expected/chm/BRD4/scripts/com/mmpbsa.sh` to understand the correct group-ID handling pattern.

    In `2_trj4mmpbsa.sh` (around lines 140-191):
    1. After creating the complex group in `index.ndx`, determine the actual group IDs for receptor, ligand, and complex groups.
    2. Write/export these group IDs so `2_mmpbsa.sh` can consume them (e.g., write to a file like `mmpbsa_groups.dat`, or echo them for capture).
    3. The group IDs should be derived by parsing `gmx make_ndx` output or by querying the generated index file.

    In `2_mmpbsa.sh` (around lines 95-99, 181-188):
    1. Replace hardcoded `-cg 1 12` (or equivalent fixed group IDs from config defaults) with dynamically resolved values.
    2. Read group IDs from whatever mechanism `2_trj4mmpbsa.sh` provides (file, variable, or index query).
    3. If config explicitly provides group IDs AND they are non-default, honor the config override as an escape hatch.
    4. Add validation: if group IDs cannot be resolved, fail with a clear error message rather than silently using wrong groups.

    IMPORTANT: Preserve existing error handling, logging, and Slurm submission logic. Only modify group-ID resolution.
  </action>
  <verify>
    1. `grep -n "cg\|group.*id\|mmpbsa_group" scripts/com/2_mmpbsa.sh` shows dynamic group resolution
    2. `grep -n "group.*id\|make_ndx\|ndx" scripts/com/2_trj4mmpbsa.sh` shows group ID extraction
    3. `bash -n scripts/com/2_mmpbsa.sh && bash -n scripts/com/2_trj4mmpbsa.sh` passes syntax check
  </verify>
  <done>
    - Group IDs are dynamically resolved from index file, not hardcoded
    - Config override exists as escape hatch for manual group specification
    - Both scripts pass bash syntax check
  </done>
</task>

</tasks>

<verification>
- Both scripts pass `bash -n` syntax check
- No hardcoded group IDs remain (grep confirms dynamic resolution)
- Error handling present for unresolvable groups
</verification>

<success_criteria>
- MM/PBSA group selection adapts to actual index ordering
- Backward compatible with explicit config overrides
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-02-SUMMARY.md`
</output>
