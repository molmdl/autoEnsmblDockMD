---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 03
subsystem: docking
tags: [dock2com, gro, topology, posre, validation, bash, python]

requires:
  - phase: 5.1-critical-correctness-and-namespace-fixes
    provides: Baseline correctness fixes and active remediation context for dock/complex stages
provides:
  - dock2com wrappers now create `com.gro` by merging receptor and ligand GRO coordinates before validation
  - merged complex GRO preserves receptor box vectors and correct combined atom count
  - position-restraint generation validates GRO↔ITP atom count and atom-order/name alignment before writing `posre_lig.itp`
affects: [5.1-05, 5.1-06, docking, complex-md, scientific-validity]

tech-stack:
  added: []
  patterns: ["Wrapper-level artifact creation before require_file contract checks", "Fail-fast GRO↔ITP atom mapping validation before restraint generation"]

key-files:
  created: [.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-03-SUMMARY.md]
  modified: [scripts/dock/4_dock2com.sh, scripts/dock/4_dock2com_ref.sh, scripts/dock/4_dock2com_2.2.1.py]

key-decisions:
  - "Derive receptor GRO from docking SDF prefix and merge receptor+ligand coordinates in wrappers to satisfy com.gro artifact contract"
  - "Treat any GRO↔ITP atom-count or atom-name/order mismatch as a hard error to prevent silent misapplied position restraints"

patterns-established:
  - "Docking wrappers must produce validated complex coordinate artifacts before downstream copying/requirements"
  - "Safety checks that guard scientific correctness should fail fast, not warn-and-continue"

duration: 2 min
completed: 2026-04-19
---

# Phase 5.1 Plan 03: Dock2com Artifact Contract and Atom-Order Validation Summary

**Dock2com wrappers now deterministically build `com.gro` from receptor+ligand coordinates before requiring it, and ligand restraint generation now hard-validates GRO↔ITP atom mapping to prevent wrong-atom restraints.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-19T10:07:44Z
- **Completed:** 2026-04-19T10:10:34Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added reusable GRO merge logic to both dock2com wrappers so `com.gro` is created before `require_file "${com_gro}"` checks.
- Added receptor GRO resolution from SDF prefix (e.g., `recX-lig.sdf` → `recX.pdb_ali.gro`) and fail-fast error handling when required merge inputs are missing.
- Added GRO↔ITP validation in `4_dock2com_2.2.1.py` (atom count and atom names/order), with non-zero exit on mismatch.
- Wired wrappers to pass `--itp` to the posre utility so validation is enforced in normal pipeline usage.

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix com.gro creation in dock2com wrappers** - `a69df99` (fix)
2. **Task 2: Add ligand GRO↔ITP atom-order validation** - `65f4f33` (fix)

## Files Created/Modified
- `scripts/dock/4_dock2com.sh` - Added receptor GRO discovery, GRO merge generation, and validated posre invocation with ligand ITP.
- `scripts/dock/4_dock2com_ref.sh` - Mirrored complex GRO creation and validation flow for reference-ligand workflow.
- `scripts/dock/4_dock2com_2.2.1.py` - Added ITP parser and strict GRO↔ITP validation before position-restraint generation.
- `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-03-SUMMARY.md` - Execution summary for this plan.

## Decisions Made
- Kept complex coordinate assembly in shell wrappers (where receptor and ligand file context already exists), instead of widening Python helper scope.
- Enforced mismatch as hard failure rather than warning, because incorrect restraint index mapping is a scientific correctness risk.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Enforced ITP validation in wrapper invocations**
- **Found during:** Task 2 (atom-order validation implementation)
- **Issue:** Adding validation logic in Python utility alone would not protect wrapper-driven pipeline runs unless wrappers passed ligand ITP input.
- **Fix:** Updated both wrappers to call `4_dock2com_2.2.1.py` with `--itp "${itp_path}"`.
- **Files modified:** `scripts/dock/4_dock2com.sh`, `scripts/dock/4_dock2com_ref.sh`
- **Verification:** Wrapper syntax checks pass and updated invocations include `--itp`.
- **Committed in:** `a69df99` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Additional wiring was required to ensure the planned safety validation is actually active in pipeline execution.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Ready for `5.1-05-PLAN.md` and `5.1-06-PLAN.md`.
- No blockers from this plan.

---
*Phase: 5.1-critical-correctness-and-namespace-fixes*
*Completed: 2026-04-19*
