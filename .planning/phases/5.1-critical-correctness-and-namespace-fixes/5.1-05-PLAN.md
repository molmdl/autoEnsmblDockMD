---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - .opencode/skills/aedmd-checker-validate/SKILL.md
  - .opencode/skills/aedmd-com-analyze/SKILL.md
  - .opencode/skills/aedmd-com-md/SKILL.md
  - .opencode/skills/aedmd-com-mmpbsa/SKILL.md
  - .opencode/skills/aedmd-com-setup/SKILL.md
  - .opencode/skills/aedmd-debugger-diagnose/SKILL.md
  - .opencode/skills/aedmd-dock-run/SKILL.md
  - .opencode/skills/aedmd-orchestrator-resume/SKILL.md
  - .opencode/skills/aedmd-rec-ensemble/SKILL.md
  - .opencode/skills/aedmd-status/SKILL.md
  - scripts/commands/aedmd-checker-validate.sh
  - scripts/commands/aedmd-com-analyze.sh
  - scripts/commands/aedmd-com-md.sh
  - scripts/commands/aedmd-com-mmpbsa.sh
  - scripts/commands/aedmd-com-setup.sh
  - scripts/commands/aedmd-debugger-diagnose.sh
  - scripts/commands/aedmd-dock-run.sh
  - scripts/commands/aedmd-orchestrator-resume.sh
  - scripts/commands/aedmd-rec-ensemble.sh
  - scripts/commands/aedmd-status.sh
  - scripts/commands/common.sh
  - AGENTS.md
autonomous: true

must_haves:
  truths:
    - "All skills use aedmd-* prefix in folder names and SKILL.md frontmatter"
    - "All command wrappers use aedmd-* prefix in filenames"
    - "AGENTS.md references aedmd-* prefixed skills and commands"
    - "No generic /status or /dock-run names remain"
  artifacts:
    - path: ".opencode/skills/aedmd-status/SKILL.md"
      provides: "Namespaced status skill"
      contains: "aedmd-status"
    - path: "scripts/commands/aedmd-status.sh"
      provides: "Namespaced status command wrapper"
  key_links:
    - from: "AGENTS.md"
      to: ".opencode/skills/aedmd-*/SKILL.md"
      via: "skill path references"
      pattern: "aedmd-"
---

<objective>
Rename all 10 skills and command wrappers from generic names to `aedmd-*` prefix to prevent namespace collisions with OpenCode platform commands.

Purpose: Generic names like `/status` conflict with OpenCode internals. `aedmd-` prefix establishes project namespace.
Output: All skills, commands, and AGENTS.md updated with `aedmd-*` prefix.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/code_analysis/2026-04-19-skill-doc-conflicts-report.md
@AGENTS.md
@scripts/commands/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename skill directories and update SKILL.md frontmatter</name>
  <files>.opencode/skills/aedmd-*/SKILL.md (10 new directories)</files>
  <action>
    For each of the 10 skills (checker-validate, com-analyze, com-md, com-mmpbsa, com-setup, debugger-diagnose, dock-run, orchestrator-resume, rec-ensemble, status):

    1. Create new directory `.opencode/skills/aedmd-{name}/`
    2. Copy `SKILL.md` from old directory to new directory
    3. In each new SKILL.md, update the `name:` field in YAML frontmatter from `{name}` to `aedmd-{name}`
    4. Update any self-references within the SKILL.md body
    5. After all new directories are created and verified, remove the old directories

    The 10 renames:
    - checker-validate → aedmd-checker-validate
    - com-analyze → aedmd-com-analyze
    - com-md → aedmd-com-md
    - com-mmpbsa → aedmd-com-mmpbsa
    - com-setup → aedmd-com-setup
    - debugger-diagnose → aedmd-debugger-diagnose
    - dock-run → aedmd-dock-run
    - orchestrator-resume → aedmd-orchestrator-resume
    - rec-ensemble → aedmd-rec-ensemble
    - status → aedmd-status

    IMPORTANT: Use `git mv` for the directory renames to preserve git history.
  </action>
  <verify>
    1. `ls .opencode/skills/ | grep aedmd` shows 10 aedmd-prefixed directories
    2. `ls .opencode/skills/ | grep -v aedmd` shows no old directories (except possibly README or other non-skill files)
    3. `grep -l "^name:" .opencode/skills/aedmd-*/SKILL.md | wc -l` shows 10 files with name field
  </verify>
  <done>
    - 10 skill directories renamed to aedmd-* prefix
    - All SKILL.md frontmatter name fields updated
    - Old directories removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename command wrappers and update references</name>
  <files>scripts/commands/aedmd-*.sh, scripts/commands/common.sh, AGENTS.md</files>
  <action>
    1. Rename all 10 command wrapper scripts using `git mv`:
       - checker-validate.sh → aedmd-checker-validate.sh
       - com-analyze.sh → aedmd-com-analyze.sh
       - com-md.sh → aedmd-com-md.sh
       - com-mmpbsa.sh → aedmd-com-mmpbsa.sh
       - com-setup.sh → aedmd-com-setup.sh
       - debugger-diagnose.sh → aedmd-debugger-diagnose.sh
       - dock-run.sh → aedmd-dock-run.sh
       - orchestrator-resume.sh → aedmd-orchestrator-resume.sh
       - rec-ensemble.sh → aedmd-rec-ensemble.sh
       - status.sh → aedmd-status.sh

    2. In each renamed wrapper, update any internal references to skill names or paths to use the `aedmd-` prefix.

    3. In `scripts/commands/common.sh`, update any references to old skill/command names.

    4. In `AGENTS.md`:
       - Update the Slash Commands table to use `aedmd-*` names (e.g., `/aedmd-status`, `/aedmd-dock-run`)
       - Update all skill path references from `.opencode/skills/{name}/` to `.opencode/skills/aedmd-{name}/`
       - Update the Skills section references

    IMPORTANT: Do NOT update README.md, docs/GUIDE.md, docs/EXPERIMENTAL.md, or .opencode/docs/AGENT-WORKFLOW.md here — those are handled in Plan 06.
  </action>
  <verify>
    1. `ls scripts/commands/*.sh | grep -v common | grep -v aedmd` shows no old-named wrappers
    2. `grep -c "aedmd-" AGENTS.md` shows multiple aedmd references
    3. `bash -n scripts/commands/common.sh` passes
  </verify>
  <done>
    - 10 command wrappers renamed with aedmd-* prefix
    - common.sh references updated
    - AGENTS.md fully updated with aedmd-* names and paths
    - No old generic names remain in AGENTS.md or commands
  </done>
</task>

</tasks>

<verification>
- `ls .opencode/skills/` shows only aedmd-* skill directories
- `ls scripts/commands/` shows only aedmd-* wrappers (plus common.sh)
- AGENTS.md uses aedmd-* throughout
- No old names remain in renamed files
</verification>

<success_criteria>
- Complete namespace migration to aedmd-* prefix
- No naming collisions with OpenCode platform commands
- AGENTS.md accurately reflects new names and paths
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-05-SUMMARY.md`
</output>
