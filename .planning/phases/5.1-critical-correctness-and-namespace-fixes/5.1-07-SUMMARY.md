---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 07
subsystem: infra
tags: [bash, commands, status, config, workspace]

# Dependency graph
requires:
  - phase: 5.1-critical-correctness-and-namespace-fixes
    provides: Prior correctness and namespace remediations for command wrappers and docs
provides:
  - aedmd-status reads mode from the canonical [docking] section
  - aedmd-status supports --workdir before workspace detection
affects: [next-phase-selection, command-reliability, config-consistency]

# Tech tracking
tech-stack:
  added: []
  patterns: [config-template alignment, pre-bootstrap custom-flag parsing]

key-files:
  created: []
  modified: [scripts/commands/aedmd-status.sh]

key-decisions:
  - "Use filtered argument pass-through so --workdir is handled locally while existing common parse_flags behavior remains intact"

patterns-established:
  - "Status wrappers should parse wrapper-only flags before ensure_env/find_workspace_root"
  - "Config-section lookups must match scripts/config.ini.template keys exactly"

# Metrics
duration: 2 min
completed: 2026-04-19
---

# Phase 5.1 Plan 07: Status Gap Closure Summary

**aedmd-status now reads docking mode from [docking] and supports pre-root --workdir directory targeting without breaking existing common flag parsing.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-19T12:12:32Z
- **Completed:** 2026-04-19T12:15:13Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Replaced status-mode section parsing from `[workflow]` to `[docking]` in `aedmd-status.sh`.
- Added `--workdir <dir>` parsing before environment/bootstrap so workspace detection starts from the requested directory.
- Preserved `common.sh` flag handling by forwarding filtered arguments to `parse_flags` after local `--workdir` extraction.

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix config section from [workflow] to [docking]** - `f2d0aa0` (fix)
2. **Task 2: Implement --workdir flag** - `14749b9` (feat)

_Plan metadata: pending docs commit at plan completion._

## Files Created/Modified
- `scripts/commands/aedmd-status.sh` - Corrected config section parsing and added wrapper-level `--workdir` support.

## Decisions Made
- Kept `parse_flags` in use with filtered args rather than removing it, so existing shared flags (`--config`, `--verbose`, forwarded params) continue to behave consistently.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added validation for missing --workdir value**
- **Found during:** Task 2 (Implement --workdir flag)
- **Issue:** `--workdir` without a value would otherwise shift invalidly and cause ambiguous behavior
- **Fix:** Added explicit argument/value check and clear error message before shifting
- **Files modified:** scripts/commands/aedmd-status.sh
- **Verification:** `bash -n scripts/commands/aedmd-status.sh` plus successful `--workdir /tmp` run
- **Committed in:** `14749b9` (Task 2)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Tightened correctness and error handling while preserving intended behavior.

## Issues Encountered
- `scripts/setenv.sh` invokes `conda activate autoEnsmblDockMD`, which failed in this environment (`conda init` / env availability). Verification proceeded by mocking `conda` in-shell to validate script control flow and output for this command-layer change.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 5.1 plan set is now complete through 5.1-07.
- Ready for roadmap transition/next phase selection.

---
*Phase: 5.1-critical-correctness-and-namespace-fixes*
*Completed: 2026-04-19*
