---
phase: 5.1-critical-correctness-and-namespace-fixes
plan: 06
type: execute
wave: 2
depends_on: ["5.1-05"]
files_modified:
  - README.md
  - docs/GUIDE.md
  - docs/EXPERIMENTAL.md
  - .opencode/docs/AGENT-WORKFLOW.md
  - scripts/commands/common.sh
  - scripts/commands/aedmd-status.sh
autonomous: true

must_haves:
  truths:
    - "All docs reference aedmd-* prefixed skills and commands"
    - "Handoff persistence contract is consistent between docs and common.sh"
    - "Config section references are correct (status reads correct config key)"
    - "com-analyze agent role is consistent across docs"
    - "--workdir flag is either implemented or removed from docs"
  artifacts:
    - path: "README.md"
      provides: "Updated skill/command references"
    - path: "docs/GUIDE.md"
      provides: "Updated skill/command references"
  key_links:
    - from: "docs/GUIDE.md"
      to: ".opencode/skills/aedmd-*/SKILL.md"
      via: "skill name references"
      pattern: "aedmd-"
---

<objective>
Update all remaining documentation with aedmd-* namespace and fix documentation consistency issues (handoff persistence, config sections, agent roles, --workdir).

Purpose: Complete namespace migration across docs and resolve doc/code inconsistencies.
Output: Fully consistent documentation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/code_analysis/2026-04-19-skill-doc-conflicts-report.md
@.planning/code_analysis/2026-04-19-human-agent-doc-consistency-report.md
@README.md
@docs/GUIDE.md
@docs/EXPERIMENTAL.md
@.opencode/docs/AGENT-WORKFLOW.md
@scripts/commands/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs with aedmd-* namespace references</name>
  <files>README.md, docs/GUIDE.md, docs/EXPERIMENTAL.md, .opencode/docs/AGENT-WORKFLOW.md</files>
  <action>
    In each document, find and replace all references to old skill/command names with aedmd-* versions:

    1. README.md: Update any `/status`, `/dock-run`, `/com-setup` etc. references to `/aedmd-status`, `/aedmd-dock-run`, `/aedmd-com-setup` etc. Update skill path references.
    2. docs/GUIDE.md: Same namespace updates for any skill/command references.
    3. docs/EXPERIMENTAL.md: Same namespace updates.
    4. .opencode/docs/AGENT-WORKFLOW.md: Update all skill references, command dispatch tables, and HandoffRecord examples to use aedmd-* names.

    Use search-and-replace for consistency. The mapping is simply prepending `aedmd-` to each of the 10 skill/command names.
  </action>
  <verify>
    1. `grep -rn "/status\b" README.md docs/GUIDE.md docs/EXPERIMENTAL.md .opencode/docs/AGENT-WORKFLOW.md | grep -v aedmd` shows no old references (except prose about generic status concepts)
    2. `grep -c "aedmd-" README.md` shows multiple references
  </verify>
  <done>
    - All docs use aedmd-* prefixed names
    - No old generic skill/command names remain as references
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix documentation consistency issues</name>
  <files>scripts/commands/common.sh, scripts/commands/aedmd-status.sh, .opencode/docs/AGENT-WORKFLOW.md, AGENTS.md</files>
  <action>
    Address four specific doc/code consistency issues from the analysis reports:

    1. **Handoff persistence contract** (common.sh vs docs): Check if `scripts/commands/common.sh` has a `save_handoff()` or similar function that writes to `.handoffs/*.json`. If not, either:
       a. Add the function to match what docs describe, OR
       b. Update docs (AGENT-WORKFLOW.md) to accurately reflect what common.sh actually does.
       Choose whichever aligns with the simpler, correct implementation.

    2. **Config section mismatch**: The status skill/command reads `[workflow] mode` but `scripts/config.ini.template` has `[docking] mode`. Fix the status command (`aedmd-status.sh`) to read the correct config section, OR update the config template. Check which is correct by examining what other scripts use.

    3. **com-analyze agent role**: AGENTS.md says com-analyze is a "runner" skill but it should be under "analyzer" agent type. Update AGENTS.md to list com-analyze under the correct agent type (Analyzer, Agent type 3).

    4. **--workdir flag**: Check if `aedmd-status.sh` (formerly `status.sh`) documents `--workdir` but doesn't implement it. If documented but not implemented, remove the documentation reference. If easy to implement (just cd to workdir before inspection), implement it.
  </action>
  <verify>
    1. `bash -n scripts/commands/common.sh` passes
    2. `bash -n scripts/commands/aedmd-status.sh` passes
    3. `grep "com-analyze" AGENTS.md` shows it under Analyzer agent section
  </verify>
  <done>
    - Handoff persistence is consistent between docs and code
    - Config section references are correct
    - com-analyze listed under correct agent type
    - --workdir either implemented or removed from docs
  </done>
</task>

</tasks>

<verification>
- All docs pass namespace audit (no old names remain)
- Doc/code consistency issues resolved
- All shell scripts pass syntax check
</verification>

<success_criteria>
- Complete aedmd-* namespace migration across all documentation
- Zero doc/code inconsistencies from the analysis reports remain
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-critical-correctness-and-namespace-fixes/5.1-06-SUMMARY.md`
</output>
