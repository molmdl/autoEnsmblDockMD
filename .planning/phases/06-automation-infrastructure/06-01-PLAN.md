---
phase: 06-automation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/infra/plugins/__init__.py
  - scripts/infra/plugins/workspace_init.py
  - scripts/commands/aedmd-workspace-init.sh
autonomous: true

must_haves:
  truths:
    - User can create isolated workspace from template with single command
    - Workspace initialization validates required files before copying
    - Init failures emit structured errors pointing to missing inputs
  artifacts:
    - path: scripts/infra/plugins/__init__.py
      provides: Plugin package marker
      min_lines: 5
    - path: scripts/infra/plugins/workspace_init.py
      provides: Workspace initialization plugin
      exports: ["main", "initialize_workspace"]
      min_lines: 80
    - path: scripts/commands/aedmd-workspace-init.sh
      provides: Workspace init wrapper command
      min_lines: 40
  key_links:
    - from: scripts/commands/aedmd-workspace-init.sh
      to: scripts/infra/plugins/workspace_init.py
      via: python3 execution
      pattern: "python3.*workspace_init\\.py"
    - from: scripts/infra/plugins/workspace_init.py
      to: scripts/agents/schemas/handoff.py
      via: HandoffRecord import
      pattern: "from scripts\\.agents\\.schemas\\.handoff import HandoffRecord"
---

<objective>
Create plugin infrastructure and implement workspace initialization automation hook.

Purpose: Establish foundation for all Phase 6 plugins and automate workspace setup (1,500-3,000 token savings/run)
Output: Working plugin package + workspace-init command emitting HandoffRecord JSON
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-infrastructure/06-CONTEXT.md
@.planning/phases/06-automation-infrastructure/06-RESEARCH.md

# Existing patterns
@scripts/agents/schemas/handoff.py
@scripts/commands/common.sh
@scripts/commands/aedmd-status.sh

# Config template structure
@scripts/config.ini.template
</context>

<tasks>

<task type="auto">
  <name>Create plugin package infrastructure</name>
  <files>
scripts/infra/plugins/__init__.py
  </files>
  <action>
Create `scripts/infra/plugins/__init__.py` package marker.

Content:
- Module docstring: "Automation plugins for workflow support operations."
- Version constant: `__version__ = "1.0.0"`
- Export list: `__all__ = ["workspace_init", "preflight", "handoff_inspect", "group_id_check", "conversion_cache"]`

Keep minimal - this is just a package marker for import organization.
  </action>
  <verify>
python3 -c "import scripts.infra.plugins; print(scripts.infra.plugins.__version__)"
  </verify>
  <done>
Import succeeds and prints "1.0.0"
  </done>
</task>

<task type="auto">
  <name>Implement workspace initialization plugin</name>
  <files>
scripts/infra/plugins/workspace_init.py
  </files>
  <action>
Create `scripts/infra/plugins/workspace_init.py` following Pattern 1 from 06-RESEARCH.md.

Implementation requirements:
- Import HandoffRecord, HandoffStatus from scripts.agents.schemas.handoff
- Function `initialize_workspace(template_dir: Path, target_dir: Path, force: bool = False) -> HandoffRecord`
- Validation checks:
  1. Check if target_dir exists - emit HandoffStatus.BLOCKED if exists and force=False
  2. Validate template has required structure: config.ini, mdp/rec/, mdp/com/
  3. Check for critical input files in template (emit warnings if missing, not errors)
- Copy operation: Use shutil.copytree(template_dir, target_dir) with dirs_exist_ok=force
- Success HandoffRecord includes: workspace_path, template_source, created_dirs list in metadata
- main() function:
  - argparse: --template (required), --target (required), --force (flag)
  - Call initialize_workspace()
  - Print record.to_dict() as JSON to stdout
  - Exit 0 if SUCCESS, 1 if FAILURE/BLOCKED

Follow code example from 06-RESEARCH.md "Workspace Initialization with Template Copy" section.
Do NOT use `rm` commands - rely on shutil.rmtree only when force=True.
  </action>
  <verify>
python3 -m py_compile scripts/infra/plugins/workspace_init.py && \
python3 scripts/infra/plugins/workspace_init.py --template work/input --target /tmp/test_ws_init --force 2>&1 | jq -r '.status'
  </verify>
  <done>
Syntax valid, test run emits JSON with status field (success/failure/blocked)
  </done>
</task>

<task type="auto">
  <name>Create workspace-init wrapper command</name>
  <files>
scripts/commands/aedmd-workspace-init.sh
  </files>
  <action>
Create `scripts/commands/aedmd-workspace-init.sh` following Pattern 2 from 06-RESEARCH.md.

Structure (follow aedmd-status.sh pattern):
1. Shebang, set -euo pipefail
2. Source common.sh for ensure_env, find_workspace_root
3. Parse flags: --template (required), --target (required), --force (optional flag)
4. Call plugin:
   ```bash
   PLUGIN="${PROJECT_ROOT}/scripts/infra/plugins/workspace_init.py"
   HANDOFF_FILE=".handoffs/workspace_init.json"
   
   mkdir -p "$(dirname "$HANDOFF_FILE")"
   python3 "$PLUGIN" --template "$TEMPLATE" --target "$TARGET" ${FORCE:+--force} > "$HANDOFF_FILE"
   ```
5. Use check_handoff_result "workspace_init" to interpret status
6. Exit with status from check_handoff_result

Add usage function documenting --template, --target, --force flags.
  </action>
  <verify>
bash -n scripts/commands/aedmd-workspace-init.sh && \
grep -q "workspace_init.py" scripts/commands/aedmd-workspace-init.sh && \
grep -q "check_handoff_result" scripts/commands/aedmd-workspace-init.sh
  </verify>
  <done>
Syntax valid, references workspace_init.py plugin, uses check_handoff_result for status interpretation
  </done>
</task>

</tasks>

<verification>
1. Plugin package imports cleanly: `python3 -c "import scripts.infra.plugins"`
2. Workspace init plugin runs standalone: `python3 scripts/infra/plugins/workspace_init.py --help`
3. Wrapper command exists and is executable: `bash -n scripts/commands/aedmd-workspace-init.sh`
4. Handoff JSON conforms to HandoffRecord schema (has status, from_agent, to_agent, stage fields)
</verification>

<success_criteria>
1. **Plugin Infrastructure Established**
   - scripts/infra/plugins/ package exists and imports cleanly
   - Provides foundation for remaining 4 plugins

2. **Workspace Init Automation Works**
   - Single command creates isolated workspace from template
   - Validates template structure before copying
   - Emits structured HandoffRecord JSON to .handoffs/

3. **Error Handling is Clear**
   - Missing template files -> HandoffStatus.FAILURE with clear errors
   - Existing target without --force -> HandoffStatus.BLOCKED with recommendations
   - Invalid paths -> HandoffStatus.FAILURE with actionable error messages
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-infrastructure/06-01-SUMMARY.md`
</output>
