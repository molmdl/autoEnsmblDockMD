---
phase: 06-automation-infrastructure
plan: 07
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified:
  - scripts/infra/plugins/dry_run_audit.sh
  - tests/phase06_integration_test.sh
autonomous: true

must_haves:
  truths:
    - Dry-run audit verifies all Phase 6 automation hooks are correctly integrated
    - Integration test validates end-to-end automation workflow
    - Audit script is read-only and safe to run in any workspace
  artifacts:
    - path: scripts/infra/plugins/dry_run_audit.sh
      provides: Read-only Phase 6 validation audit
      min_lines: 60
    - path: tests/phase06_integration_test.sh
      provides: Integration test for automation hooks
      min_lines: 80
  key_links:
    - from: scripts/infra/plugins/dry_run_audit.sh
      to: all Phase 6 plugins and wrappers
      via: file existence checks and syntax validation
      pattern: "test -f.*plugins|bash -n"
    - from: tests/phase06_integration_test.sh
      to: scripts/commands/aedmd-*.sh
      via: wrapper execution tests
      pattern: "scripts/commands/aedmd-"
---

<objective>
Create dry-run audit script and integration tests to validate Phase 6 automation infrastructure.

Purpose: Verify all plugins, wrappers, and skills are correctly implemented and integrated before Phase 7 execution
Output: Read-only audit script + integration test suite
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-infrastructure/06-CONTEXT.md
@.planning/phases/06-automation-infrastructure/06-RESEARCH.md

# Dry-run pattern from research
@.planning/phases/06-automation-infrastructure/06-RESEARCH.md

# All Phase 6 artifacts
@scripts/infra/plugins/__init__.py
@scripts/infra/plugins/workspace_init.py
@scripts/infra/plugins/preflight.py
@scripts/infra/plugins/handoff_inspect.py
@scripts/infra/plugins/group_id_check.py
@scripts/infra/plugins/conversion_cache.py
@scripts/commands/aedmd-workspace-init.sh
@scripts/commands/aedmd-preflight.sh
@scripts/commands/aedmd-handoff-inspect.sh
@scripts/commands/aedmd-group-id-check.sh
@.opencode/plugins/workspace-init.js
@.opencode/plugins/preflight.js
@.opencode/plugins/handoff-inspect.js
@.opencode/plugins/group-id-check.js
@.opencode/plugins/conversion-cache.js
@.opencode/skills/aedmd-workspace-init/SKILL.md
@.opencode/skills/aedmd-preflight/SKILL.md
@.opencode/skills/aedmd-handoff-inspect/SKILL.md
@.opencode/skills/aedmd-group-id-check/SKILL.md
@.opencode/skills/aedmd-conversion-cache/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Create dry-run audit script</name>
  <files>
scripts/infra/plugins/dry_run_audit.sh
  </files>
  <action>
Create `scripts/infra/plugins/dry_run_audit.sh` following "Pattern 4: Dry-Run Audit Script" from 06-RESEARCH.md.

Structure:
1. Shebang: `#!/usr/bin/env bash`
2. set -euo pipefail
3. Header: "=== Phase 6 Automation Infrastructure Dry-Run Audit ==="

4. **Check Python plugins exist and syntax valid:**
   ```bash
   PLUGINS=(
       "scripts/infra/plugins/__init__.py"
       "scripts/infra/plugins/workspace_init.py"
       "scripts/infra/plugins/preflight.py"
       "scripts/infra/plugins/handoff_inspect.py"
       "scripts/infra/plugins/group_id_check.py"
       "scripts/infra/plugins/conversion_cache.py"
   )
   
   for plugin in "${PLUGINS[@]}"; do
       if [[ -f "$plugin" ]]; then
           echo "[OK] $plugin exists"
           python3 -m py_compile "$plugin" 2>/dev/null && echo "  ✓ Syntax valid" || echo "  ✗ Syntax error"
       else
           echo "[MISSING] $plugin"
       fi
   done
   ```

5. **Check bash wrapper scripts:**
   ```bash
   WRAPPERS=(
       "scripts/commands/aedmd-workspace-init.sh"
       "scripts/commands/aedmd-preflight.sh"
       "scripts/commands/aedmd-handoff-inspect.sh"
       "scripts/commands/aedmd-group-id-check.sh"
   )
   
   for wrapper in "${WRAPPERS[@]}"; do
       if [[ -f "$wrapper" ]]; then
           echo "[OK] $wrapper exists"
           bash -n "$wrapper" 2>/dev/null && echo "  ✓ Syntax valid" || echo "  ✗ Syntax error"
       else
           echo "[MISSING] $wrapper"
       fi
   done
   ```

6. **Check OpenCode plugins:**
   ```bash
   OC_PLUGINS=(
       ".opencode/plugins/workspace-init.js"
       ".opencode/plugins/preflight.js"
       ".opencode/plugins/handoff-inspect.js"
       ".opencode/plugins/group-id-check.js"
       ".opencode/plugins/conversion-cache.js"
   )
   
   for plugin in "${OC_PLUGINS[@]}"; do
       if [[ -f "$plugin" ]]; then
           echo "[OK] $plugin exists"
           grep -q "definePlugin" "$plugin" && echo "  ✓ Uses SDK" || echo "  ✗ Missing SDK"
       else
           echo "[MISSING] $plugin"
       fi
   done
   ```

7. **Check skill documentation:**
   ```bash
   SKILLS=(
       ".opencode/skills/aedmd-workspace-init/SKILL.md"
       ".opencode/skills/aedmd-preflight/SKILL.md"
       ".opencode/skills/aedmd-handoff-inspect/SKILL.md"
       ".opencode/skills/aedmd-group-id-check/SKILL.md"
       ".opencode/skills/aedmd-conversion-cache/SKILL.md"
   )
   
   for skill in "${SKILLS[@]}"; do
       if [[ -f "$skill" ]]; then
           echo "[OK] $skill exists"
           grep -q "token_savings" "$skill" && echo "  ✓ Documented" || echo "  ✗ Missing token_savings"
       else
           echo "[MISSING] $skill"
       fi
   done
   ```

8. Footer: "Audit complete. Review output for missing files or syntax errors."

Make script executable: `chmod +x scripts/infra/plugins/dry_run_audit.sh`

This is READ-ONLY - no modifications, just inspection.
  </action>
  <verify>
bash -n scripts/infra/plugins/dry_run_audit.sh && \
test -x scripts/infra/plugins/dry_run_audit.sh && \
./scripts/infra/plugins/dry_run_audit.sh 2>&1 | grep -q "Audit complete"
  </verify>
  <done>
Audit script syntax valid, executable, runs successfully and reports completion
  </done>
</task>

<task type="auto">
  <name>Create Phase 6 integration test</name>
  <files>
tests/phase06_integration_test.sh
  </files>
  <action>
Create `tests/phase06_integration_test.sh` to validate end-to-end automation workflow.

Structure:
1. Shebang, set -euo pipefail
2. Source scripts/setenv.sh to activate conda env
3. Create test workspace in /tmp/phase06_test_$$

4. **Test 1: Workspace initialization**
   ```bash
   echo "Test 1: Workspace init"
   mkdir -p /tmp/phase06_template/mdp/{rec,com}
   cp scripts/config.ini.template /tmp/phase06_template/config.ini
   
   scripts/commands/aedmd-workspace-init.sh \
       --template /tmp/phase06_template \
       --target /tmp/phase06_test_$$ \
       --force
   
   test -f /tmp/phase06_test_$$/config.ini && echo "  ✓ Workspace created" || { echo "  ✗ FAILED"; exit 1; }
   ```

5. **Test 2: Preflight validation**
   ```bash
   echo "Test 2: Preflight validation"
   cd /tmp/phase06_test_$$
   scripts/commands/aedmd-preflight.sh --config config.ini
   
   test -f .handoffs/preflight_validation.json && echo "  ✓ Preflight ran" || { echo "  ✗ FAILED"; exit 1; }
   ```

6. **Test 3: Handoff inspection (after preflight)**
   ```bash
   echo "Test 3: Handoff inspection"
   scripts/commands/aedmd-handoff-inspect.sh --workspace /tmp/phase06_test_$$
   
   test -f .handoffs/handoff_inspection.json && echo "  ✓ Inspector ran" || { echo "  ✗ FAILED"; exit 1; }
   jq -r '.data.latest_stage' .handoffs/handoff_inspection.json | grep -q "preflight" && echo "  ✓ Detected preflight" || echo "  ✗ Wrong stage"
   ```

7. **Test 4: Group ID checker (no index.ndx - should handle gracefully)**
   ```bash
   echo "Test 4: Group ID checker"
   scripts/commands/aedmd-group-id-check.sh --workspace /tmp/phase06_test_$$
   
   # Should emit FAILURE status since no index.ndx
   jq -r '.status' .handoffs/group_id_check.json | grep -q "failure" && echo "  ✓ Handled missing index" || echo "  ✗ Wrong status"
   ```

8. **Test 5: ConversionCache library import**
   ```bash
   echo "Test 5: ConversionCache import"
   python3 -c "from scripts.infra.plugins.conversion_cache import ConversionCache; from pathlib import Path; c = ConversionCache(Path('/tmp')); print('OK')"
   echo "  ✓ ConversionCache imports"
   ```

9. **Cleanup:**
   ```bash
   echo "Cleanup"
   rm -rf /tmp/phase06_test_$$ /tmp/phase06_template
   echo "All tests passed!"
   ```

Make executable: `chmod +x tests/phase06_integration_test.sh`
  </action>
  <verify>
bash -n tests/phase06_integration_test.sh && \
test -x tests/phase06_integration_test.sh && \
grep -q "Workspace init" tests/phase06_integration_test.sh && \
grep -q "Preflight validation" tests/phase06_integration_test.sh
  </verify>
  <done>
Integration test script syntax valid, executable, contains all 5 test cases
  </done>
</task>

</tasks>

<verification>
1. Dry-run audit script exists and is executable: `test -x scripts/infra/plugins/dry_run_audit.sh`
2. Audit runs without errors: `./scripts/infra/plugins/dry_run_audit.sh`
3. Integration test exists and is executable: `test -x tests/phase06_integration_test.sh`
4. Integration test can be run (may fail if plugins not yet implemented, but script structure valid): `bash -n tests/phase06_integration_test.sh`
</verification>

<success_criteria>
1. **Dry-Run Audit Validates All Artifacts**
   - Checks existence of 6 Python plugins
   - Validates syntax of 4 bash wrappers
   - Verifies 5 OpenCode plugins use definePlugin SDK
   - Confirms 5 skill files exist with token_savings metadata

2. **Integration Test Covers Workflow**
   - Test 1: Workspace initialization creates isolated workspace
   - Test 2: Preflight validation runs and emits handoff JSON
   - Test 3: Handoff inspector parses latest handoff
   - Test 4: Group ID checker handles missing inputs gracefully
   - Test 5: ConversionCache library imports successfully

3. **Read-Only Safety**
   - Audit script performs no modifications
   - Integration test uses /tmp for isolated testing
   - Cleanup removes test artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-infrastructure/06-07-SUMMARY.md`
</output>
