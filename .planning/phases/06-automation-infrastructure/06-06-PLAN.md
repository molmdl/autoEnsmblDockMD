---
phase: 06-automation-infrastructure
plan: 06
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - .opencode/skills/aedmd-workspace-init/SKILL.md
  - .opencode/skills/aedmd-preflight/SKILL.md
  - .opencode/skills/aedmd-handoff-inspect/SKILL.md
  - .opencode/skills/aedmd-group-id-check/SKILL.md
  - .opencode/skills/aedmd-conversion-cache/SKILL.md
autonomous: true

must_haves:
  truths:
    - Markdown skills document automation hooks for universal agent compatibility
    - Skills reference both bash wrapper commands and OpenCode plugins
    - Dual-format architecture complete: Markdown (universal) + OpenCode plugins (native)
  artifacts:
    - path: .opencode/skills/aedmd-workspace-init/SKILL.md
      provides: Workspace init skill documentation
      contains: "aedmd-workspace-init"
      min_lines: 40
    - path: .opencode/skills/aedmd-preflight/SKILL.md
      provides: Preflight validation skill documentation
      contains: "aedmd-preflight"
      min_lines: 40
    - path: .opencode/skills/aedmd-handoff-inspect/SKILL.md
      provides: Handoff inspector skill documentation
      contains: "aedmd-handoff-inspect"
      min_lines: 40
    - path: .opencode/skills/aedmd-group-id-check/SKILL.md
      provides: Group ID checker skill documentation
      contains: "aedmd-group-id-check"
      min_lines: 40
    - path: .opencode/skills/aedmd-conversion-cache/SKILL.md
      provides: Conversion cache skill documentation
      contains: "aedmd-conversion-cache"
      min_lines: 40
  key_links:
    - from: .opencode/skills/*/SKILL.md
      to: scripts/commands/aedmd-*.sh
      via: wrapper command reference
      pattern: "scripts/commands/aedmd-.*\\.sh"
    - from: .opencode/skills/*/SKILL.md
      to: .opencode/plugins/*.js
      via: OpenCode plugin reference
      pattern: "\\.opencode/plugins/.*\\.js"
---

<objective>
Create Markdown skill files for all 5 automation hooks, completing dual-format architecture.

Purpose: Provide universal agent compatibility via Markdown skills while maintaining OpenCode native plugin efficiency
Output: 5 SKILL.md files with YAML frontmatter documenting both bash wrappers and OpenCode plugins
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automation-infrastructure/06-CONTEXT.md
@.planning/phases/06-automation-infrastructure/06-RESEARCH.md

# User requirement: DUAL-FORMAT architecture
# Markdown skills = universal compatibility (fallback)
# OpenCode plugins = efficient native operation (primary)

# Existing skill format
@.opencode/skills/aedmd-status/SKILL.md

# Bash wrappers created in Plans 01-04
@scripts/commands/aedmd-workspace-init.sh
@scripts/commands/aedmd-preflight.sh
@scripts/commands/aedmd-handoff-inspect.sh
@scripts/commands/aedmd-group-id-check.sh

# OpenCode plugins created in Plan 05
@.opencode/plugins/workspace-init.js
@.opencode/plugins/preflight.js
@.opencode/plugins/handoff-inspect.js
@.opencode/plugins/group-id-check.js
@.opencode/plugins/conversion-cache.js
</context>

<tasks>

<task type="auto">
  <name>Create workspace-init skill documentation</name>
  <files>
.opencode/skills/aedmd-workspace-init/SKILL.md
  </files>
  <action>
Create `.opencode/skills/aedmd-workspace-init/SKILL.md` following existing SKILL.md format from aedmd-status.

YAML frontmatter:
```yaml
---
name: aedmd-workspace-init
description: Use when creating isolated workspace from template for workflow execution
license: MIT
compatibility: Requires python 3.10+, conda environment
metadata:
  author: autoEnsmblDockMD
  version: "1.0"
  agent: none
  stage: workspace_initialization
  token_savings: "1500-3000"
---
```

Sections:
1. **When to use this skill**: Creating isolated workspace, copying from work/input template, validating workspace structure
2. **Prerequisites**: Template directory exists with config.ini and mdp/ subdirectories
3. **Usage**:
   - Bash wrapper: `scripts/commands/aedmd-workspace-init.sh --template work/input --target work/test`
   - OpenCode plugin: `.opencode/plugins/workspace-init.js` via plugin system
4. **Parameters table**: template (required), target (required), force (optional)
5. **Expected Output**: HandoffRecord JSON with workspace_path, template_source, created_dirs
6. **Troubleshooting**: Template missing files, target already exists, permission errors

Reference both bash wrapper AND OpenCode plugin for dual-format support.
  </action>
  <verify>
test -f .opencode/skills/aedmd-workspace-init/SKILL.md && \
grep -q "name: aedmd-workspace-init" .opencode/skills/aedmd-workspace-init/SKILL.md && \
grep -q "workspace-init.js" .opencode/skills/aedmd-workspace-init/SKILL.md
  </verify>
  <done>
Skill file exists with YAML frontmatter and references both bash wrapper and OpenCode plugin
  </done>
</task>

<task type="auto">
  <name>Create preflight skill documentation</name>
  <files>
.opencode/skills/aedmd-preflight/SKILL.md
  </files>
  <action>
Create `.opencode/skills/aedmd-preflight/SKILL.md`.

YAML frontmatter:
```yaml
---
name: aedmd-preflight
description: Use when validating config, tools, and inputs before workflow execution
license: MIT
compatibility: Requires python 3.10+, gmx/gnina/gmx_MMPBSA for full validation
metadata:
  author: autoEnsmblDockMD
  version: "1.0"
  agent: none
  stage: preflight_validation
  token_savings: "2000-4000"
---
```

Sections:
1. **When to use**: Before running any workflow stage, after config changes, validating new workspace
2. **Prerequisites**: config.ini exists, workspace initialized
3. **Usage**:
   - Bash: `scripts/commands/aedmd-preflight.sh --config config.ini`
   - Plugin: `.opencode/plugins/preflight.js`
4. **Parameters**: config (required), workspace (optional)
5. **Expected Output**: Validation results with ERROR/WARNING/INFO severity, mode-specific checks
6. **Troubleshooting**: Mode-aware validation, missing sections, tool availability

Highlight mode-aware validation: targeted mode requires reference_ligand, blind mode needs autobox parameters.
  </action>
  <verify>
test -f .opencode/skills/aedmd-preflight/SKILL.md && \
grep -q "name: aedmd-preflight" .opencode/skills/aedmd-preflight/SKILL.md && \
grep -q "token_savings" .opencode/skills/aedmd-preflight/SKILL.md
  </verify>
  <done>
Preflight skill exists with frontmatter and dual-format references
  </done>
</task>

<task type="auto">
  <name>Create remaining skill documentation files</name>
  <files>
.opencode/skills/aedmd-handoff-inspect/SKILL.md
.opencode/skills/aedmd-group-id-check/SKILL.md
.opencode/skills/aedmd-conversion-cache/SKILL.md
  </files>
  <action>
Create 3 remaining SKILL.md files following established pattern.

**aedmd-handoff-inspect/SKILL.md:**
- name: aedmd-handoff-inspect
- description: Parse latest handoff and provide next-action guidance
- token_savings: "1200-2500"
- When to use: After stage execution, debugging workflow, resume from checkpoint
- Parameters: workspace (optional)
- Expected output: Latest stage, normalized status, next-action recommendation

**aedmd-group-id-check/SKILL.md:**
- name: aedmd-group-id-check
- description: Validate MM/PBSA group IDs against index.ndx before MM/PBSA execution
- token_savings: "900-1800"
- When to use: Before running MM/PBSA stage, after trajectory preparation
- Parameters: workspace (optional)
- Expected output: Group ID validation results, receptor/ligand group IDs

**aedmd-conversion-cache/SKILL.md:**
- name: aedmd-conversion-cache
- description: Manage file conversion cache with staleness detection
- token_savings: "1000-2000"
- When to use: Before file conversions (SDF→GRO, GRO→MOL2), cache clearing
- Parameters: workspace, operation (get/put/clear), sourceFile, targetFormat
- Expected output: Cache hit/miss/stale status, cached file path if fresh
- Note: This is a library plugin, not a standalone command - used by conversion scripts

All follow YAML frontmatter format with name, description, license, compatibility, metadata (including token_savings).

Reference both bash wrappers (where applicable) and OpenCode plugins.
  </action>
  <verify>
for skill in aedmd-handoff-inspect aedmd-group-id-check aedmd-conversion-cache; do
  test -f .opencode/skills/$skill/SKILL.md && grep -q "name: $skill" .opencode/skills/$skill/SKILL.md || exit 1
done && echo "OK"
  </verify>
  <done>
All 3 skill files exist with YAML frontmatter and dual-format references
  </done>
</task>

</tasks>

<verification>
1. All 5 skill directories created: ls -d .opencode/skills/aedmd-{workspace-init,preflight,handoff-inspect,group-id-check,conversion-cache}
2. All have SKILL.md files: ls .opencode/skills/aedmd-*/SKILL.md
3. All have YAML frontmatter: head -5 .opencode/skills/aedmd-*/SKILL.md | grep "^name:"
4. All reference token_savings: grep "token_savings" .opencode/skills/aedmd-*/SKILL.md
5. All reference both bash wrappers and OpenCode plugins (where applicable)
</verification>

<success_criteria>
1. **Dual-Format Architecture Complete**
   - Markdown skills provide universal compatibility (any coding agent can read)
   - OpenCode plugins provide native efficiency (OpenCode can execute programmatically)
   - Both formats document the same 5 automation hooks

2. **Skill Documentation Consistent**
   - All follow YAML frontmatter format from existing skills
   - All include: name, description, license, compatibility, metadata
   - All document: when to use, prerequisites, usage, parameters, output, troubleshooting

3. **Token Savings Documented**
   - Each skill's frontmatter includes estimated token_savings range
   - Aggregate savings: 6,600-13,300 tokens per run-support cycle
</success_criteria>

<output>
After completion, create `.planning/phases/06-automation-infrastructure/06-06-SUMMARY.md`
</output>
