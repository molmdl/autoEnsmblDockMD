---
phase: 04-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/skills/rec-ensemble/SKILL.md
  - .opencode/skills/dock-run/SKILL.md
  - .opencode/skills/com-setup/SKILL.md
  - .opencode/skills/com-md/SKILL.md
  - .opencode/skills/com-mmpbsa/SKILL.md
  - .opencode/skills/com-analyze/SKILL.md
  - .opencode/skills/checker-validate/SKILL.md
  - .opencode/skills/debugger-diagnose/SKILL.md
  - .opencode/skills/orchestrator-resume/SKILL.md
  - .opencode/skills/status/SKILL.md
autonomous: true

must_haves:
  truths:
    - "Agents can discover and load skill files at runtime"
    - "Each skill describes when to use it, what it does, and how to invoke it"
    - "Skills follow Agent Skills standard (YAML frontmatter + Markdown body)"
    - "Skills are under 500 lines each (progressive disclosure)"
  artifacts:
    - path: ".opencode/skills/dock-run/SKILL.md"
      provides: "Docking execution skill metadata and instructions"
      contains: "name: dock-run"
    - path: ".opencode/skills/rec-ensemble/SKILL.md"
      provides: "Receptor ensemble skill"
      contains: "name: rec-ensemble"
  key_links:
    - from: ".opencode/skills/*/SKILL.md"
      to: "scripts/commands/*.sh"
      via: "references command scripts for execution"
      pattern: "scripts/commands/"
    - from: ".opencode/skills/*/SKILL.md"
      to: "scripts/agents/"
      via: "references agent dispatch"
      pattern: "runner|checker|analyzer|debugger|orchestrator"
---

<objective>
Create Agent Skills files for all 10 workflow commands following the OpenCode skill format (https://opencode.ai/docs/skills).

Purpose: Skills provide progressive-disclosure metadata so agents know when/how to use each command. They are discovered from .opencode/skills/ by OpenCode (also supports .agents/skills/ and .claude/skills/).

Output: 10 SKILL.md files in .opencode/skills/{command-name}/ directories
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-integration/04-CONTEXT.md
@.planning/phases/04-integration/04-RESEARCH.md
@WORKFLOW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline stage skills (6 files)</name>
  <files>
    .opencode/skills/rec-ensemble/SKILL.md
    .opencode/skills/dock-run/SKILL.md
    .opencode/skills/com-setup/SKILL.md
    .opencode/skills/com-md/SKILL.md
    .opencode/skills/com-mmpbsa/SKILL.md
    .opencode/skills/com-analyze/SKILL.md
  </files>
  <action>
Create 6 SKILL.md files for pipeline stages. Each follows Agent Skills standard format:

```markdown
---
name: {command-name}
description: {One clear sentence describing when to use this skill. Include specific keywords users/agents would mention. 1-1024 chars.}
license: MIT
compatibility: Requires gnina, gromacs 2022+, python 3.10+
metadata:
  author: autoEnsmblDockMD
  version: "1.0"
  agent: {runner|analyzer}
  stage: {stage_enum_value}
---

NOTE on OpenCode compatibility: Only `name`, `description`, `license`, `compatibility`, `metadata` are recognized frontmatter fields. The `metadata` map must be string-to-string. The `name` must be lowercase alphanumeric with single hyphens (regex: ^[a-z0-9]+(-[a-z0-9]+)*$), 1-64 chars, and MUST match the parent directory name.

# {Stage Title}

{One paragraph: what this does}

## When to use this skill
- {3-4 bullet points: specific triggers}

## Prerequisites
- {What must exist before running}
- {Config sections required}

## Usage
Command: `scripts/commands/{name}.sh --config config.ini`
Agent dispatch: `python -m scripts.agents --agent {agent} --input handoff.json`

## Parameters
| Parameter | Config Key | CLI Flag | Default | Description |
|-----------|-----------|----------|---------|-------------|
{stage-specific params from WORKFLOW.md and config}

## Expected Output
- {Files produced}
- {Handoff record location}

## Troubleshooting
- {2-3 common issues and fixes}
```

Stage details to include:
- **rec-ensemble**: Receptor preparation + MD + clustering + alignment. Agent: runner. Prerequisites: PDB file, config [rec] section. Output: aligned ensemble PDBs.
- **dock-run**: gnina docking (targeted or blind per config mode). Agent: runner. Prerequisites: receptor ensemble, ligand, config [dock] section. Output: docked poses.
- **com-setup**: Complex system preparation (topology, solvation, ions). Agent: runner. Prerequisites: docked poses, config [complex] section. Output: GROMACS topology + coordinates.
- **com-md**: Production MD simulation. Agent: runner. Prerequisites: prepared complex system, config [md] section. Output: trajectory files.
- **com-mmpbsa**: MM/PBSA binding energy calculation. Agent: runner. Prerequisites: MD trajectory, config [mmpbsa] section. Output: energy decomposition.
- **com-analyze**: Trajectory analysis (RMSD, RMSF, contacts). Agent: analyzer. Prerequisites: MD trajectory. Output: analysis plots + data.

Consult WORKFLOW.md for accurate parameter lists and prerequisite details. Keep each file under 150 lines.
  </action>
  <verify>
for d in .opencode/skills/rec-ensemble .opencode/skills/dock-run .opencode/skills/com-setup .opencode/skills/com-md .opencode/skills/com-mmpbsa .opencode/skills/com-analyze; do
  test -f "$d/SKILL.md" && echo "OK: $d/SKILL.md"
done
  </verify>
  <done>6 pipeline SKILL.md files exist with correct YAML frontmatter (name matches directory) and Markdown body</done>
</task>

<task type="auto">
  <name>Task 2: Create utility agent skills (4 files)</name>
  <files>
    .opencode/skills/checker-validate/SKILL.md
    .opencode/skills/debugger-diagnose/SKILL.md
    .opencode/skills/orchestrator-resume/SKILL.md
    .opencode/skills/status/SKILL.md
  </files>
  <action>
Create 4 SKILL.md files for utility commands. Same Agent Skills format as Task 1.

- **checker-validate**: On-demand result validation. Agent: checker. When: after any stage completes, user suspects quality issues. Reads handoff records + output files, produces validation report.
- **debugger-diagnose**: Error diagnosis. Agent: debugger. When: stage fails, unexpected output. Reads error logs + handoff records, suggests fixes. Aware of GROMACS/gnina/gmx_MMPBSA versions.
- **orchestrator-resume**: Resume workflow from last checkpoint. Agent: orchestrator. When: session interrupted, user returns to continue. Reads checkpoint state, determines next stage.
- **status**: Workspace inspection (no agent dispatch). When: user wants to see current state. Shows mode, completed stages, pending stages, last handoff status. Reference scripts/commands/status.sh.

Keep each under 100 lines — these are simpler than pipeline skills.
  </action>
  <verify>
for d in .opencode/skills/checker-validate .opencode/skills/debugger-diagnose .opencode/skills/orchestrator-resume .opencode/skills/status; do
  test -f "$d/SKILL.md" && echo "OK: $d/SKILL.md"
done
  </verify>
  <done>4 utility SKILL.md files exist with correct frontmatter and consistent format</done>
</task>

</tasks>

<verification>
- All 10 SKILL.md files exist in .opencode/skills/{name}/ directories
- Each has valid YAML frontmatter with `name` matching directory name
- Each has sections: When to use, Prerequisites, Usage, Parameters (pipeline only), Troubleshooting
- No file exceeds 500 lines
- Skills reference correct agent names and stage enum values
</verification>

<success_criteria>
- 10 Agent Skills files discoverable from .opencode/skills/
- Consistent format across all skills (Agent Skills standard)
- Skills reference correct command scripts and agent dispatch
- Progressive disclosure: metadata is scannable, details are structured
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration/04-02-SUMMARY.md`
</output>
