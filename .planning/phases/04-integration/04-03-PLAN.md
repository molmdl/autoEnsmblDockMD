---
phase: 04-integration
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - docs/EXPERIMENTAL.md
autonomous: false

must_haves:
  truths:
    - "A command script can dispatch to the agent CLI and receive structured output"
    - "Agent support is clearly marked as experimental"
    - "Skills are discoverable from .github/skills/ directory"
  artifacts:
    - path: "docs/EXPERIMENTAL.md"
      provides: "Experimental status documentation for agent features"
      contains: "experimental"
  key_links:
    - from: "scripts/commands/*.sh"
      to: "python -m scripts.agents"
      via: "CLI dispatch"
      pattern: "python -m scripts\\.agents"
    - from: ".github/skills/*/SKILL.md"
      to: "scripts/commands/*.sh"
      via: "usage references"
      pattern: "scripts/commands"
---

<objective>
Smoke-test the integration between command scripts, agent CLI, and skills. Document experimental status of agent features.

Purpose: Verify the command→agent dispatch chain works end-to-end. Create experimental status documentation required by AGENT-07.

Output: Validated integration + docs/EXPERIMENTAL.md
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-integration/04-CONTEXT.md
@.planning/phases/04-integration/04-01-SUMMARY.md
@.planning/phases/04-integration/04-02-SUMMARY.md
@scripts/agents/__main__.py
@scripts/commands/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test</name>
  <files>scripts/commands/common.sh, scripts/commands/status.sh</files>
  <action>
Run integration smoke tests to verify the command→agent dispatch chain:

1. Source setenv.sh: `source scripts/setenv.sh`
2. Syntax check all command scripts: `for f in scripts/commands/*.sh; do bash -n "$f"; done`
3. Test status command in a test workspace: Create temp dir with a minimal config.ini, run `scripts/commands/status.sh --config config.ini` from it. Verify it prints workspace info without errors.
4. Test agent dispatch dry-run: Run `python -m scripts.agents --help` to verify CLI entrypoint works.
5. Verify skill discovery: Check all 10 .github/skills/*/SKILL.md exist and have valid YAML frontmatter (grep for `^name:` in each).

If any command script has issues (bad common.sh sourcing, missing function, etc.), fix them.
If the agent CLI --input flag doesn't match what command scripts expect, update command scripts to match actual CLI interface.

Do NOT run actual pipeline stages (no GROMACS/gnina needed). This is a wiring test only.
  </action>
  <verify>
bash -n scripts/commands/common.sh
python -m scripts.agents --help 2>&1 | head -5
ls .github/skills/*/SKILL.md | wc -l  # Should be 10
  </verify>
  <done>All command scripts pass syntax check, agent CLI responds to --help, all 10 skills discoverable</done>
</task>

<task type="auto">
  <name>Task 2: Create experimental status documentation</name>
  <files>docs/EXPERIMENTAL.md</files>
  <action>
Create `docs/EXPERIMENTAL.md` documenting the experimental status of agent features (requirement AGENT-07).

Content:
1. **Header**: "Agent Support — Experimental" with clear warning banner
2. **What's experimental**: Agent-based workflow orchestration, slash commands dispatching to agents, skill-based capability loading
3. **What's stable**: Shell scripts (Phase 2), configuration system (Phase 1), manual workflow execution
4. **Known limitations**: Agents require OpenCode or compatible agent system; handoff schema may change; skill format follows agentskills.io v1 (may evolve)
5. **How to use without agents**: Direct script invocation (reference Phase 2 scripts)
6. **Feedback**: How to report issues

Keep under 80 lines. Professional tone. No emojis.
  </action>
  <verify>test -f docs/EXPERIMENTAL.md && wc -l docs/EXPERIMENTAL.md</verify>
  <done>docs/EXPERIMENTAL.md exists, clearly marks agent features as experimental, under 80 lines</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete integration layer: 11 command bridge scripts, 10 Agent Skills files, experimental documentation. Command scripts dispatch to Python agent CLI via JSON handoff. Skills follow agentskills.io standard.</what-built>
  <how-to-verify>
1. Check command scripts: `ls scripts/commands/` — should show 11 .sh files
2. Check skills: `ls .github/skills/` — should show 10 directories each with SKILL.md
3. Spot-check one command: `cat scripts/commands/dock-run.sh` — should source common.sh, call dispatch_agent with "runner" and "dock_run"
4. Spot-check one skill: `cat .github/skills/dock-run/SKILL.md` — should have YAML frontmatter with name: dock-run
5. Review docs/EXPERIMENTAL.md for accuracy
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All command scripts pass bash syntax check
- Agent CLI responds to --help
- All 10 skills discoverable in .github/skills/
- docs/EXPERIMENTAL.md exists and is accurate
- No broken references between commands, skills, and agent CLI
</verification>

<success_criteria>
- Integration chain verified: command → common.sh → agent CLI → handoff
- Experimental status documented per AGENT-07
- Human verified the integration layer
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration/04-03-SUMMARY.md`
</output>
