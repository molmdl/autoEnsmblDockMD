---
phase: 02-core-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/rec/0_prep.sh
  - scripts/rec/1_pr_rec.sh
  - scripts/rec/3_ana.sh
  - scripts/rec/4_cluster.sh
  - scripts/rec/5_align.py
autonomous: true

must_haves:
  truths:
    - "User can prepare receptor ensemble from PDB with single command per step"
    - "Scripts work for both AMBER and CHARMM force fields via config"
    - "Ensemble structures are aligned to reference complex (Mode A)"
  artifacts:
    - path: "scripts/rec/0_prep.sh"
      provides: "System preparation: pdb2pqr, pdb2gmx, solvation, ions, minimization, equilibration"
      contains: "source.*common.sh"
    - path: "scripts/rec/1_pr_rec.sh"
      provides: "Submit parallel production MD trials"
      contains: "sbatch"
    - path: "scripts/rec/3_ana.sh"
      provides: "Trajectory conversion and basic analysis"
      contains: "trjconv"
    - path: "scripts/rec/4_cluster.sh"
      provides: "GROMOS clustering to extract ensemble"
      contains: "gmx cluster"
    - path: "scripts/rec/5_align.py"
      provides: "Align ensemble to reference complex"
      contains: "argparse"
  key_links:
    - from: "scripts/rec/0_prep.sh"
      to: "scripts/infra/common.sh"
      via: "source"
      pattern: "source.*common.sh"
---

<objective>
Generalize all receptor ensemble generation scripts from expected/ templates.

Purpose: Receptor ensemble is the first pipeline stage — all downstream stages depend on it.
Output: 5 scripts in scripts/rec/ supporting both AMBER and CHARMM force fields via config.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-CONTEXT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-01-SUMMARY.md
@expected/chm/BRD4/scripts/rec/0_prep.sh
@expected/chm/BRD4/scripts/rec/1_pr_rec.sh
@expected/chm/BRD4/scripts/rec/3_ana.sh
@expected/chm/BRD4/scripts/rec/4_cluster.sh
@expected/chm/BRD4/scripts/rec/align_structures.py
@expected/amb/scripts/rec/README.md
@scripts/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize receptor bash scripts</name>
  <files>scripts/rec/0_prep.sh, scripts/rec/1_pr_rec.sh, scripts/rec/3_ana.sh, scripts/rec/4_cluster.sh</files>
  <action>
Create generalized versions of the 4 receptor bash scripts by adapting expected/chm/BRD4/scripts/rec/ templates.

All scripts must:
- Source `$(dirname $0)/../infra/common.sh`
- Call `init_script <name> "$@"` to handle --config, --help flags
- Read mode-specific parameters from config (force field, water model, partition, ntomp, etc.)
- Support both AMBER and CHARMM via config `[receptor] ff = amber19sb` or `ff = charmm36`
- Use `run_gmx` wrapper for all GROMACS commands
- Use `submit_job` for Slurm submissions
- Include `-h` / `--help` with usage description

**0_prep.sh**: pdb2pqr → pdb2gmx → editconf → solvate → genion → minimize → equilibrate. Config-driven: ff, water model, box distance, ion concentration, partition, GPU count.

**1_pr_rec.sh**: Submit N parallel production MD trials (default 4). Config: n_trials, md_time, partition. Use job arrays or loop with sbatch.

**3_ana.sh**: trjconv (center, fit, PBC), RMSD, RMSF calculations. Config: analysis selections.

**4_cluster.sh**: gmx cluster with gromos method, extract representative structures as rec0.gro through recN.gro. Config: cutoff, ensemble_size (default 10).

Reference the existing expected/ scripts closely — they contain tested GROMACS command sequences. Add config/CLI flexibility on top.
  </action>
  <verify>Each script runs with `--help` without errors. `bash -n scripts/rec/0_prep.sh` passes syntax check for all 4.</verify>
  <done>4 receptor bash scripts created, all source common.sh, all support --help and --config flags</done>
</task>

<task type="auto">
  <name>Task 2: Create ensemble alignment script</name>
  <files>scripts/rec/5_align.py</files>
  <action>
Generalize expected/chm/BRD4/scripts/rec/align_structures.py into scripts/rec/5_align.py.

Must:
- Use argparse with: `--structures` (glob or list of GRO/PDB), `--reference` (reference complex PDB), `--output-dir`, `--selection` (default "backbone")
- Use MDAnalysis for alignment (Universe, align.AlignTo or alignto)
- Align all ensemble structures to reference complex structure
- Save aligned structures preserving original filenames with prefix or in output dir
- Support both CLI and library import (`if __name__ == '__main__'`)
- Add `--config` flag to read parameters from INI file [receptor] section
- Print summary: N structures aligned, RMSD before/after

This is Mode A specific but should work generically for any ensemble alignment task.
  </action>
  <verify>`python scripts/rec/5_align.py --help` shows usage. `python -c "from scripts.rec import align_structures"` or syntax check passes.</verify>
  <done>5_align.py aligns ensemble to reference, supports CLI + library mode, uses MDAnalysis</done>
</task>

</tasks>

<verification>
- All 5 scripts exist in scripts/rec/
- `bash -n` syntax check passes for all .sh files
- `python -m py_compile scripts/rec/5_align.py` passes
- Each script supports --help flag
</verification>

<success_criteria>
- Complete receptor ensemble pipeline from PDB to aligned ensemble
- Both AMBER and CHARMM force fields supported via config
- Scripts follow numeric prefix convention (0_, 1_, 3_, 4_, 5_)
- All scripts source common.sh and use config_loader
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-02-SUMMARY.md`
</output>
