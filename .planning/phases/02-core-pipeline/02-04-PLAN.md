---
phase: 02-core-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/dock/1_rec4dock.sh
  - scripts/dock/2_gnina.sh
autonomous: true

must_haves:
  truths:
    - "User can copy receptor ensemble to docking directory"
    - "User can run gnina docking in both blind and targeted modes via config"
    - "Ensemble docking submits parallel Slurm jobs per ligand"
  artifacts:
    - path: "scripts/dock/1_rec4dock.sh"
      provides: "Copy/link ensemble conformations to dock directory"
      contains: "source.*common.sh"
    - path: "scripts/dock/2_gnina.sh"
      provides: "Unified gnina docking: blind, targeted, test redocking"
      contains: "gnina"
  key_links:
    - from: "scripts/dock/2_gnina.sh"
      to: "config.ini [docking]"
      via: "get_config docking mode"
      pattern: "mode.*blind\\|targeted\\|test"
---

<objective>
Create docking execution scripts supporting both blind and targeted modes.

Purpose: Ensemble docking is the core computational step — must support multiple modes via config.
Output: 2 scripts: receptor preparation for docking + unified gnina execution.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-01-SUMMARY.md
@expected/chm/BRD4/scripts/dock/1_rec4dock.sh
@expected/chm/BRD4/scripts/dock/2_gnina_blind.sh
@expected/amb/scripts/dock/gnina_test.sh
@expected/amb/scripts/dock/gnina_0.sh
@expected/amb/scripts/dock/gnina_1.sh
@scripts/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize rec4dock and unified gnina script</name>
  <files>scripts/dock/1_rec4dock.sh, scripts/dock/2_gnina.sh</files>
  <action>
**1_rec4dock.sh** (from expected/chm/BRD4/scripts/dock/1_rec4dock.sh):
- Copy/symlink ensemble structures (rec0-recN.gro/pdb) to dock/ directory
- Convert GRO to PDB if needed (gnina requires PDB)
- Source common.sh, config-driven ensemble_size
- Support --config and --help

**2_gnina.sh** — UNIFY 4 Mode A scripts (gnina_test.sh, gnina_0.sh, gnina_1.sh, gnina_2.sh) + Mode B (2_gnina_blind.sh) into one script with `--mode` flag:

Modes (from config `[docking] mode`):
- `test`: Single receptor + reference ligand validation redocking (gnina_test.sh)
- `blind`: Ensemble × ligand, autobox from receptor (2_gnina_blind.sh)
- `targeted`: Ensemble × ligand, autobox from reference ligand (gnina_0/1/2.sh)

Config parameters `[docking]`:
- mode = blind|targeted|test
- exhaustiveness = 64 (Mode A) or 100 (Mode B)
- num_modes = 32
- autobox_add = 12 (targeted) or 4 (blind)
- autobox_ligand = ref.pdb (targeted) or receptor (blind)
- scoring = (default: CNN, optional: ad4_scoring)
- addH = off, stripH = off
- cpu = 8

Must:
- Submit one Slurm job per ligand (parallel via loop or job array)
- Each job docks against all ensemble members (inner loop)
- Log scores and output SDF files
- Handle ligand discovery from directory listing or config list

Reference expected/amb/scripts/dock/gnina_*.sh carefully for the exact gnina flags used in each mode — they differ in autobox_ligand and autobox_add.
  </action>
  <verify>`bash -n scripts/dock/1_rec4dock.sh && bash -n scripts/dock/2_gnina.sh` passes. `scripts/dock/2_gnina.sh --help` shows mode options.</verify>
  <done>Unified gnina script supports blind/targeted/test modes, parallel Slurm submission per ligand</done>
</task>

</tasks>

<verification>
- Both scripts exist and pass syntax check
- 2_gnina.sh --help shows all three modes
- Config parameters documented in help output
</verification>

<success_criteria>
- Single gnina script replaces 5 mode-specific scripts
- Mode-specific parameters (autobox, exhaustiveness) driven by config
- Parallel job submission via Slurm for multi-ligand docking
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-04-SUMMARY.md`
</output>
