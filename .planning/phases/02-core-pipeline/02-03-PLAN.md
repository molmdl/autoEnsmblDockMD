---
phase: 02-core-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/dock/0_gro2mol2.sh
  - scripts/dock/0_gro_itp_to_mol2.py
  - scripts/dock/0_sdf2gro.sh
autonomous: true

must_haves:
  truths:
    - "User can convert ligands between formats needed for docking pipeline"
    - "AMBER ligands with angle type issues are handled correctly"
    - "Both GRO→MOL2 and SDF→GRO conversions available"
  artifacts:
    - path: "scripts/dock/0_gro2mol2.sh"
      provides: "CHARMM GRO to MOL2 conversion wrapper"
      contains: "source.*common.sh"
    - path: "scripts/dock/0_gro_itp_to_mol2.py"
      provides: "AMBER GRO+ITP to MOL2 conversion with angle type handling"
      min_lines: 200
    - path: "scripts/dock/0_sdf2gro.sh"
      provides: "Batch SDF to GRO conversion"
      contains: "obabel\\|gmx"
  key_links:
    - from: "scripts/dock/0_gro_itp_to_mol2.py"
      to: "expected/amb/scripts/dock/gro_itp_to_mol2.py"
      via: "Generalized from existing 659-line utility"
      pattern: "def.*mol2"
---

<objective>
Create ligand format conversion scripts for the docking stage.

Purpose: Docking requires MOL2 input; post-docking requires GRO. These converters bridge format gaps.
Output: 3 scripts in scripts/dock/ for ligand format conversion.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-01-SUMMARY.md
@expected/chm/BRD4/scripts/dock/0_gro2mol2.sh
@expected/amb/scripts/dock/gro_itp_to_mol2.py
@expected/amb/scripts/dock/sdf2gro.sh
@scripts/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize ligand conversion shell scripts</name>
  <files>scripts/dock/0_gro2mol2.sh, scripts/dock/0_sdf2gro.sh</files>
  <action>
Generalize from expected/ templates:

**0_gro2mol2.sh** (from expected/chm/BRD4/scripts/dock/0_gro2mol2.sh):
- Convert GRO files to MOL2 format for gnina docking input
- Source common.sh, support --config and --help
- Config: ligand directory, output directory
- Handle multiple ligands in loop
- Use obabel or gmx for conversion

**0_sdf2gro.sh** (from expected/amb/scripts/dock/sdf2gro.sh):
- Convert SDF docking output poses to GRO format for MD setup
- Source common.sh, support --config and --help
- Config: input SDF pattern, output directory
- Handle batch conversion across multiple ligands/receptors

Both scripts must validate inputs (require_file/require_dir) and log progress.
  </action>
  <verify>`bash -n scripts/dock/0_gro2mol2.sh && bash -n scripts/dock/0_sdf2gro.sh` passes. Both show help with --help.</verify>
  <done>Both conversion shell scripts created with config support and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Generalize AMBER GRO+ITP to MOL2 converter</name>
  <files>scripts/dock/0_gro_itp_to_mol2.py</files>
  <action>
Generalize expected/amb/scripts/dock/gro_itp_to_mol2.py (659 lines) into scripts/dock/0_gro_itp_to_mol2.py.

DO NOT rewrite from scratch — this script has extensive edge case handling for AMBER atom types, charge parsing, and bond type mapping that took significant debugging.

Changes to make:
1. Add argparse CLI: `--gro`, `--itp`, `--output`, `--config`, `--help`
2. Add library mode: functions importable via `from scripts.dock.gro_itp_to_mol2 import convert`
3. Clean up hardcoded paths → use arguments
4. Add logging via Python logging module
5. Add input validation (file existence, format checks)
6. Preserve ALL edge case handling from original

The core conversion logic (atom type mapping, charge extraction, bond detection) must remain intact from the original.
  </action>
  <verify>`python scripts/dock/0_gro_itp_to_mol2.py --help` shows usage. `python -m py_compile scripts/dock/0_gro_itp_to_mol2.py` passes.</verify>
  <done>659-line GRO+ITP to MOL2 converter generalized with CLI interface, original edge cases preserved</done>
</task>

</tasks>

<verification>
- All 3 scripts exist in scripts/dock/
- Syntax checks pass for all files
- Each supports --help
</verification>

<success_criteria>
- Ligand format conversion works for both AMBER and CHARMM workflows
- Complex AMBER conversion preserves all edge case handling from original
- Scripts follow project conventions (source common.sh, config support)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-03-SUMMARY.md`
</output>
