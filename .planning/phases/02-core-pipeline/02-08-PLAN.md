---
phase: 02-core-pipeline
plan: 08
type: execute
wave: 4
depends_on: ["02-07"]
files_modified:
  - scripts/com/1_pr_prod.sh
  - scripts/com/2_trj4mmpbsa.sh
  - scripts/com/2_run_mmpbsa.sh
  - scripts/com/2_mmpbsa.sh
  - scripts/com/2_sub_mmpbsa.sh
autonomous: true

must_haves:
  truths:
    - "User can submit production MD and MM/PBSA with simple commands"
    - "Trajectory is correctly processed for MM/PBSA (complex-only extraction)"
    - "MM/PBSA jobs submit as parallel Slurm job arrays"
  artifacts:
    - path: "scripts/com/1_pr_prod.sh"
      provides: "Equilibration + production MD submission"
      contains: "sbatch"
    - path: "scripts/com/2_trj4mmpbsa.sh"
      provides: "Trajectory center/fit/extract for MM/PBSA"
      contains: "trjconv"
    - path: "scripts/com/2_run_mmpbsa.sh"
      provides: "Unified MM/PBSA orchestration"
      contains: "gmx_MMPBSA\\|source.*common"
    - path: "scripts/com/2_mmpbsa.sh"
      provides: "Single MM/PBSA chunk execution"
      contains: "gmx_MMPBSA"
    - path: "scripts/com/2_sub_mmpbsa.sh"
      provides: "Submit MM/PBSA job array to Slurm"
      contains: "sbatch.*array"
  key_links:
    - from: "scripts/com/2_run_mmpbsa.sh"
      to: "scripts/com/2_trj4mmpbsa.sh"
      via: "calls trj processing before submission"
      pattern: "trj4mmpbsa"
---

<objective>
Create production MD and MM/PBSA calculation scripts.

Purpose: Production MD generates trajectories; MM/PBSA calculates binding energies — the final quantitative output.
Output: 5 scripts for MD production and MM/PBSA pipeline.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-07-SUMMARY.md
@expected/chm/BRD4/scripts/com/1_pr_prod.sh
@expected/chm/BRD4/scripts/com/2_run_mmpbsa.sh
@expected/chm/BRD4/scripts/com/trj4mmpbsa.sh
@expected/chm/BRD4/scripts/com/mmpbsa.sh
@expected/amb/scripts/com/pr_prod.sh
@expected/amb/scripts/com/sub_mmpbsa.sh
@expected/amb/scripts/com/mmpbsa.sh
@expected/amb/scripts/com/trj4mmpbsa.sh
@scripts/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create production MD script</name>
  <files>scripts/com/1_pr_prod.sh</files>
  <action>
Unify Mode A (pr_prod.sh) and Mode B (1_pr_prod.sh):
1. Position-restrained equilibration (pr0, pr stages with decreasing restraints)
2. Production MD submission
3. Source common.sh, config-driven

Config `[production]`: md_time, ntomp, partition, n_equilibration_stages
Config `[complex]`: ff (affects MDP parameters)

Submit via Slurm with proper GPU flags. Support multi-ligand via --ligand flag.
Handle job dependencies: equilibration must complete before production.

Reference both expected/ versions for exact MDP parameter sequences.
  </action>
  <verify>`bash -n scripts/com/1_pr_prod.sh` passes. `--help` shows usage.</verify>
  <done>Unified production MD script supports both modes, submits equilibration → production chain</done>
</task>

<task type="auto">
  <name>Task 2: Create MM/PBSA pipeline scripts</name>
  <files>scripts/com/2_trj4mmpbsa.sh, scripts/com/2_run_mmpbsa.sh, scripts/com/2_mmpbsa.sh, scripts/com/2_sub_mmpbsa.sh</files>
  <action>
**2_trj4mmpbsa.sh** (unify both modes):
- Create index file with receptor + ligand complex group
- Center and fit trajectory (trjconv with rot+trans)
- Split trajectory into N chunks for parallel MM/PBSA
- Config: n_chunks (default 4), selection groups

**2_mmpbsa.sh** (single chunk execution):
- Run gmx_MMPBSA on one trajectory chunk
- Accept chunk index as argument
- Use mmpbsa.in input file from config or template
- Handle both AMBER and CHARMM topology formats

**2_sub_mmpbsa.sh** (Slurm submission):
- Submit MM/PBSA jobs as Slurm job array (one per chunk)
- Config: partition, n_chunks
- Capture job IDs for dependency tracking

**2_run_mmpbsa.sh** (orchestrator - unifies Mode A sub_mmpbsa.sh + Mode B 2_run_mmpbsa.sh):
- Orchestrate: call trj4mmpbsa → sub_mmpbsa
- Single entry point for the entire MM/PBSA workflow
- Source common.sh, support --config, --help, --ligand

All scripts source common.sh. Reference expected/ versions closely for trjconv flags and gmx_MMPBSA invocation.
  </action>
  <verify>`bash -n` passes for all 4 scripts. Each supports --help.</verify>
  <done>Complete MM/PBSA pipeline: trajectory processing → chunk splitting → parallel submission → orchestration</done>
</task>

</tasks>

<verification>
- All 5 scripts exist in scripts/com/
- Syntax checks pass
- All support --help
</verification>

<success_criteria>
- Production MD submits with proper equilibration chain
- MM/PBSA trajectory processing handles PBC correctly
- Parallel job array submission for MM/PBSA chunks
- Both AMBER and CHARMM topologies supported
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-08-SUMMARY.md`
</output>
