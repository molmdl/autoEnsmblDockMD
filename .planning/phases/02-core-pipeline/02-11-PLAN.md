---
phase: 02-core-pipeline
plan: 11
type: execute
wave: 5
depends_on: ["02-02", "02-04", "02-06", "02-08", "02-09", "02-10"]
files_modified:
  - scripts/run_pipeline.sh
  - scripts/config.ini.template
  - scripts/CONTEXT.md
autonomous: false

must_haves:
  truths:
    - "User can see all available pipeline stages via wrapper --help"
    - "User can run individual stages or full pipeline via wrapper"
    - "Template config file documents all available parameters"
    - "scripts/CONTEXT.md reflects actual implemented inventory"
  artifacts:
    - path: "scripts/run_pipeline.sh"
      provides: "Pipeline orchestration wrapper"
      contains: "source.*common.sh"
    - path: "scripts/config.ini.template"
      provides: "Template config with all parameters documented"
      contains: "\\[receptor\\]"
    - path: "scripts/CONTEXT.md"
      provides: "Updated script inventory"
      contains: "IMPLEMENTED"
  key_links:
    - from: "scripts/run_pipeline.sh"
      to: "scripts/rec/0_prep.sh"
      via: "stage dispatch"
      pattern: "rec.*prep\\|stage.*receptor"
    - from: "scripts/run_pipeline.sh"
      to: "scripts/config.ini.template"
      via: "default config template"
      pattern: "config.*template\\|config.*ini"
---

<objective>
Create pipeline wrapper script, config template, and update script inventory.

Purpose: Provide single entry point for the entire pipeline and validate all scripts work together.
Output: Pipeline wrapper, config template, updated CONTEXT.md.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-CONTEXT.md
@scripts/CONTEXT.md
@scripts/infra/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline wrapper and config template</name>
  <files>scripts/run_pipeline.sh, scripts/config.ini.template</files>
  <action>
**run_pipeline.sh**:
- Source common.sh
- Accept: `--stage <name>` (rec_prep, rec_prod, rec_ana, rec_cluster, rec_align, dock_convert, dock_run, dock_report, dock2com, com_prep, com_prod, com_mmpbsa, com_ana, com_fp, com_archive)
- Accept: `--config <file>` (required)
- Accept: `--ligand <id>` (for per-ligand stages)
- Accept: `--help` (list all stages with descriptions)
- Accept: `--list-stages` (machine-readable stage list)
- Accept: `--dry-run` (show what would be executed)
- Dispatch to appropriate script in rec/, dock/, or com/
- Validate config file exists and has required sections for the chosen stage
- Log stage start/end with timestamps

**config.ini.template**:
- Complete template with ALL config sections and parameters used across all scripts
- Each parameter has a comment explaining purpose and valid values
- Sections: [general], [slurm], [receptor], [docking], [complex], [production], [mmpbsa], [analysis]
- Sensible defaults for mgpu cluster
- Both AMBER and CHARMM example values shown in comments

  </action>
  <verify>`bash -n scripts/run_pipeline.sh` passes. `scripts/run_pipeline.sh --help` lists all stages. config.ini.template has all sections.</verify>
  <done>Pipeline wrapper dispatches to all stage scripts, config template documents all parameters</done>
</task>

<task type="auto">
  <name>Task 2: Update script inventory</name>
  <files>scripts/CONTEXT.md</files>
  <action>
Update scripts/CONTEXT.md to reflect the actual implemented script inventory after Phase 2.

- Mark all implemented scripts with file paths and brief descriptions
- Remove GAP markers for scripts that have been implemented
- Note any remaining gaps (should be none for v1 scope)
- Update naming convention section to reflect final numeric prefixes
- Add section documenting the config.ini.template parameter reference
  </action>
  <verify>scripts/CONTEXT.md reflects all scripts in scripts/rec/, scripts/dock/, scripts/com/</verify>
  <done>CONTEXT.md accurately reflects complete script inventory</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete pipeline: 39 scripts across rec/, dock/, com/ stages with unified wrapper and config template</what-built>
  <how-to-verify>
    1. Run `scripts/run_pipeline.sh --help` — verify all stages listed
    2. Run `scripts/run_pipeline.sh --list-stages` — verify machine-readable output
    3. Review `scripts/config.ini.template` — verify all parameters documented
    4. Spot-check 2-3 scripts with `--help` flag
    5. Verify directory structure: `ls scripts/rec/ scripts/dock/ scripts/com/`
    6. Copy config template, try `scripts/run_pipeline.sh --stage rec_prep --config config.ini --dry-run`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Pipeline wrapper exists and dispatches to all stages
- Config template has all sections with documented parameters
- CONTEXT.md updated with complete inventory
- All scripts pass syntax checks
</verification>

<success_criteria>
- Single entry point for entire pipeline via run_pipeline.sh
- Config template enables user to configure all parameters
- Script inventory accurate and complete
- Human verification of overall pipeline structure
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-11-SUMMARY.md`
</output>
