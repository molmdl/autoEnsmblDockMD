---
phase: 02-core-pipeline
plan: 07
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - scripts/com/0_prep.sh
  - scripts/com/bypass_angle_type3.py
autonomous: true

must_haves:
  truths:
    - "User can prepare complex system for MD from dock2com output"
    - "AMBER angle type 3 incompatibility is handled automatically"
    - "Both AMBER and CHARMM complex preparation supported"
  artifacts:
    - path: "scripts/com/0_prep.sh"
      provides: "Complex solvation, ionization, minimization"
      contains: "source.*common.sh"
    - path: "scripts/com/bypass_angle_type3.py"
      provides: "AMBER angle type 3→1 conversion for gmx_MMPBSA compatibility"
      min_lines: 100
  key_links:
    - from: "scripts/com/0_prep.sh"
      to: "scripts/com/bypass_angle_type3.py"
      via: "python call for AMBER mode"
      pattern: "bypass_angle"
---

<objective>
Create complex MD preparation scripts: system setup and AMBER angle type fixing.

Purpose: Prepare docked complexes for MD simulation — solvation, ions, minimization.
Output: 2 scripts for complex system preparation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-01-SUMMARY.md
@expected/chm/BRD4/scripts/com/0_prep.sh
@expected/amb/scripts/com/prep_com.sh
@expected/amb/scripts/com/prep.sh
@expected/amb/scripts/com/bypass_angle_type3.py
@scripts/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified complex prep script</name>
  <files>scripts/com/0_prep.sh</files>
  <action>
Unify Mode A (prep_com.sh + prep.sh) and Mode B (0_prep.sh) into one script.

Steps (all config-driven):
1. **Complex assembly** (from Mode A prep_com.sh): Combine receptor GRO + ligand GRO → complex.gro. Build sys.top with force field includes.
2. **For AMBER mode only**: Call bypass_angle_type3.py on ligand ITP
3. **Solvation**: editconf (box), solvate
4. **Ionization**: grompp + genion (neutral, 0.15M NaCl)
5. **Minimization**: grompp + mdrun (em)
6. **Equilibration**: position-restrained MD (pr_pos)
7. Submit heavy computation steps via Slurm

Config `[complex]`: ff, water_model, box_distance, ion_conc, partition, ntomp
Config `[complex]`: mode = amber|charmm (determines angle type fixing, FF includes)

Source common.sh. Support --config, --help, --ligand (which ligand to prepare).
Handle multi-ligand: accept --ligand flag or loop from config list.

Reference both expected/chm/BRD4/scripts/com/0_prep.sh and expected/amb/scripts/com/prep.sh for the exact GROMACS command sequences — they differ in FF handling.
  </action>
  <verify>`bash -n scripts/com/0_prep.sh` passes. `scripts/com/0_prep.sh --help` shows usage.</verify>
  <done>Unified complex prep supports both AMBER and CHARMM, handles angle type fixing</done>
</task>

<task type="auto">
  <name>Task 2: Generalize bypass_angle_type3.py</name>
  <files>scripts/com/bypass_angle_type3.py</files>
  <action>
Generalize expected/amb/scripts/com/bypass_angle_type3.py (191 lines).

DO NOT rewrite — preserve core logic exactly. Add:
1. Argparse: --input (ligand ITP), --output (modified ITP, default: bypass_<input>), --topology (main .top to update), --dry-run, --help
2. Library mode: `modify_ligand_itp(input_file, output_file)` importable
3. Logging: report count of modified angles
4. Validation: check input files exist, ITP format valid
5. --config flag support for INI-based invocation

The angle type 3→1 conversion logic MUST remain identical to the original.
  </action>
  <verify>`python scripts/com/bypass_angle_type3.py --help` shows usage. `python -m py_compile scripts/com/bypass_angle_type3.py` passes.</verify>
  <done>bypass_angle_type3.py generalized with CLI, original conversion logic preserved</done>
</task>

</tasks>

<verification>
- Both scripts exist
- Syntax checks pass
- Both support --help
</verification>

<success_criteria>
- Complex preparation works for both AMBER and CHARMM force fields
- AMBER angle type incompatibility handled transparently
- Single entry point replaces 3 mode-specific scripts
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-07-SUMMARY.md`
</output>
