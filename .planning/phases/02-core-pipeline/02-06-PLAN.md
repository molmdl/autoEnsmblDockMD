---
phase: 02-core-pipeline
plan: 06
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - scripts/dock/4_dock2com.sh
  - scripts/dock/4_dock2com_ref.sh
autonomous: true

must_haves:
  truths:
    - "User can prepare complex topology from docking output with single command"
    - "Both reference ligand and new ligand complex topologies supported"
  artifacts:
    - path: "scripts/dock/4_dock2com.sh"
      provides: "New ligand complex topology preparation wrapper"
      contains: "source.*common.sh"
    - path: "scripts/dock/4_dock2com_ref.sh"
      provides: "Reference ligand complex topology preparation"
      contains: "source.*common.sh"
  key_links:
    - from: "scripts/dock/4_dock2com.sh"
      to: "scripts/dock/4_dock2com_2.py"
      via: "python call"
      pattern: "python.*dock2com"
---

<objective>
Create dock-to-complex shell wrapper scripts that orchestrate the Python utilities from Plan 05.

Purpose: Shell wrappers provide the human-friendly CLI for complex topology preparation.
Output: 2 bash scripts that call the dock2com Python pipeline.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-05-SUMMARY.md
@expected/amb/scripts/dock/dock2com_2.sh
@expected/amb/scripts/dock/dock2com_2_ref.sh
@expected/chm/BRD4/scripts/dock/dock2com_2.2.sh
@scripts/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dock2com shell wrappers</name>
  <files>scripts/dock/4_dock2com.sh, scripts/dock/4_dock2com_ref.sh</files>
  <action>
**4_dock2com.sh** — Unified wrapper (merges Mode A dock2com_2.sh + Mode B dock2com_2.2.sh):
- For each selected ligand/pose:
  1. Call 4_dock2com_1.py to convert SDF → GRO
  2. Call 4_dock2com_2.py to build complex topology
  3. Call 4_dock2com_2.2.1.py to generate position restraints
- Source common.sh, support --config, --help
- Config: ligand list, pose selection criteria, force field
- Handle multi-ligand batch processing (loop over ligands)
- Create output directory structure: com/LIGAND_ID/ with sys.top, complex.gro, posre_lig.itp

**4_dock2com_ref.sh** — Reference ligand variant (from expected/amb/scripts/dock/dock2com_2_ref.sh):
- Same pipeline but for reference ligand only (Mode A)
- Uses reference ligand parameters instead of docked poses
- Creates com/ref/ directory with topology
- Source common.sh, support --config, --help

Both scripts must validate that Python utilities exist before calling them.
  </action>
  <verify>`bash -n scripts/dock/4_dock2com.sh && bash -n scripts/dock/4_dock2com_ref.sh` passes. Both show help with --help.</verify>
  <done>Dock-to-complex wrappers orchestrate Python pipeline for both new and reference ligands</done>
</task>

</tasks>

<verification>
- Both scripts exist and pass syntax check
- Both support --help
- Both source common.sh
</verification>

<success_criteria>
- Complete dock-to-complex pipeline accessible via single shell commands
- Both reference and new ligand workflows supported
- Output directory structure matches expected/
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-06-SUMMARY.md`
</output>
